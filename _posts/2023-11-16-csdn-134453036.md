---
layout: "single"
title: "CTF-PWN-Heap — [Prerequisite Knowledge: Related Functions and Terminology]"
date: "2023-11-16 23:56:43 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "linux"
  - "Pwn"
  - "CTF"
  - "Heap"
series: "CTF Writeup"
excerpt: "Use the prev_size field to obtain the size and address of the previous chunk (space reuse: when a chunk is not freed, the prev_size of its next chunk can be used by the current chunk. fd is the address of the previous freed chunk, and bk is the address of the next freed chunk. malloc checks, on the one hand, that malloc's argument in the negative range is not greater than -2*MINSIZE, and that top_size minus the size of the allocated chunk is greater than MINSIZE (MINSIZE is the minimum chunk size)"
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/134453036"
csdn_article_id: "134453036"
migrated: true
permalink: /posts/csdn-134453036/
redirect_from:
  - "/en/CTF与Pwn/csdn-134453036/"
  - "/CTF与Pwn/csdn-134453036/"
---

{% raw %}
#  Heap
Allocated by the malloc, alloc, and realloc functions
"Before" a chunk refers to lower addresses; "after" a chunk refers to higher addresses
##  Allocating heap chunks
The ptmalloc2 heap manager: in plain terms it acts like a "middleman" — when a program wants to request heap space from the system, ptmalloc2 requests a very large block of space and then allocates the portion the user actually needs to the user.

## main_areana
 main_arena is actually obtained by the ptmalloc2 heap manager by interacting with the operating system kernel (larger than what it needs to request)
> Remember this firmly
> ![Image](/images/csdn/2023/csdn-134453036/image-001-313fd3287b.png)

The memory address returned by malloc is the user data part (an argument of 0 requests the minimum heap block allowed by the system; a negative argument becomes an enormous unsigned number, so it may fail)
prev_size field: if the chunk at the previous address has been freed, it records the size of the previous chunk (including the chunk header); if it has not been freed, it can be used by the previous chunk to store data. Use the prev_size field to obtain the size and address of the previous chunk (space reuse: when a chunk is not freed, the prev_size of its next chunk can be used by the current chunk. When prev_size already has a value and the next chunk is freed, prev_size is not cleared. If it has already been freed, its prev_size can also be used by the previous chunk)

size field: the size of the current heap chunk (chunk header + userdata); the size must be an integer multiple of 2*SIZE_SZ. If not:
When the request is less than 2*SIZE_SZ, allocate 2*SIZE_SZ
When the request is greater than n times 2*SIZE_SZ but less than n times 2*SIZE_SZ + SIZE_SZ, allocate n times 2*SIZE_SZ
When the request is greater than n times 2*SIZE_SZ + SIZE_SZ but less than (n+1) times 2*SIZE_SZ, allocate (n+1) times 2*SIZE_SZ
It feels a bit like rounding

The low three bits of this field have no effect on the chunk size (because the size is an integer multiple of 2*SIZE_SZ)
The third bit from the bottom, A (no_main_arena): 0 means allocated for the main thread; 1 means allocated for a non-main thread
The second bit from the bottom, M (is_mapped): 1 means from mmap; 0 means from the heap
The lowest bit, P (prev_inuse): 0 means the previous chunk has been freed; 1 means it has not been freed
64-bit minimum: 16+8+8  size: 16+8+8+1
32-bit minimum: 8+4+4    size: 8+4+4+1


user data field: 64-bit minimum: 16
                          32-bit minimum: 8
## The brk & sbrk functions
start_brk and brk represent the start address and end address of the heap respectively
Initially:
If ASLR is enabled: start_brk and brk are the same and located after the end of the data segment
If ASLR is not enabled: start_brk and brk are the same, at a random offset after the end of the data segment
The sbrk function:
Definition: passing 0 returns the current value of the brk pointer; passing num extends the current brk pointer value by num bytes (a positive argument increases it, a negative argument decreases it)
Return value: on success, brk() returns 0; otherwise it returns -1.
The brk function:
Definition: it can change the address stored in the brk pointer (i.e. the heap end address, also called the heap top); brk is set to whatever argument is passed
Return value: on success, brk() returns 0; otherwise it returns -1.

## malloc
malloc checks, on the one hand, that malloc's argument in the negative range is not greater than -2*MINSIZE, and that top_size minus the size of the allocated chunk is greater than MINSIZE (MINSIZE is the minimum chunk size)
The value malloc receives undergoes the following transformation:
(((req) + SIZE_SZ + MALLOC_ALIGN_MASK < MINSIZE)  ?      MINSIZE: ((req) + SIZE_SZ + MALLOC_ALIGN_MASK) & ！ MALLOC_ALIGN_MASK

A ternary operator:  a ? b : c
 req is the argument passed to malloc; SIZE_SZ is related to the OS bitness: 4 on 32-bit, 8 on 64-bit. MALLOC_ALIGN_MASK is also related to the OS bitness: 7 on 32-bit, 15 on 64-bit. The final value is the size (excluding flag bits) (except when the malloc argument is less than 8 and 16 — the size is always 0x10 and 0x20 respectively)
## free
1. It does not clear the user data of this heap chunk
2. It stores the pointer to this heap chunk into main_arena
If the argument is NULL, nothing is done
If the chunk corresponding to the argument has already been freed, all sorts of messy things can happen
## After free
The M state no longer exists
The head part of user data allocates fd and bk; when not freed there are no fd and bk — data is stored there. After free, the chunk is added to the corresponding free chunk management list. fd is the address of the previous freed chunk, and bk is the address of the next freed chunk

> fd_nextsize and bk_nextsize exist only after larger chunks are freed
> fd_nextsize is the address of the previous free chunk with a size different from the current chunk
> bk_nextsize is the address of the next free chunk with a size different from the current chunk
> Generally, free larger chunks are arranged in descending order of size
## top chunk
When none of the system's current free chunks (no matter which bin) can satisfy the memory size requested by the user, a portion is "cut" from the top chunk and allocated as a chunk to the user, and the top chunk's size decreases
{% endraw %}
