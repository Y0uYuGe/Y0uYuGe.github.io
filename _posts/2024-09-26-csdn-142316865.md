---
layout: "single"
title: "KOOBE: Towards Facilitating Exploit Generation of Kernel Out-Of-Bounds Write Vulnerabilities"
date: "2024-09-26 22:02:48 +0800"
categories:
  - "Kernel Vulnerability Research and Exploitation"
tags:
  - "Automated Kernel Exploitation"
  - "CVE"
  - "Heap"
excerpt: "A bit abstract ( size_p ): represents the size of the vulnerable object. ( T_p ): represents the OOB write set on path ( p ). ( f§ ): represents the set of path constraints collected while executing path ( p ). : indicates that ( size_p ) is an element of the symbolic expression set ( E ). Definition: for all ( e1, e2 \\in E ), if ( e1 ) is identical to ( e2 ), or ( e1 ) is a constant whose value can be taken by ( e2 ), then ( e1 \\leq e2 ). Explanation: this defines the comparison relation between symbolic expressions."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/142316865"
csdn_article_id: "142316865"
migrated: true
permalink: /posts/csdn-142316865/
redirect_from:
  - "/en/内核漏洞挖掘与利用/csdn-142316865/"
  - "/内核漏洞挖掘与利用/csdn-142316865/"
---

{% raw %}
# References
[KOOBE: Towards Facilitating Exploit Generation of Kernel Out-Of-Bounds Write Vulnerabilities](https://www.usenix.org/conference/usenixsecurity20/presentation/chen-weiteng)
[Paper Reading: KOOBE](https://j-kangel.github.io/2020/05/18/%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E4%B9%8BKOOBE/#%E6%BC%8F%E6%B4%9E%E5%88%86%E6%9E%90-Vulnerable-Analysis)
# Overview
This paper explores another important class of memory vulnerabilities in the Linux kernel — out-of-bounds (OOB) writes — and proposes a system called KOOBE to assist in analyzing such vulnerabilities. The research background is that the monolithic nature of modern operating system kernels leads to a continuous stream of newly discovered vulnerabilities, only some of which may be severe enough to cause a security takeover (e.g., privilege escalation). Therefore, researchers have begun developing automated exploit generation techniques (especially for Use-After-Free, UAF vulnerabilities) to help the vulnerability triage process. However, for OOB vulnerabilities, there is still a lack of effective analysis tools.

### KOOBE's Design Philosophy

KOOBE's design is based on two observations:
1. Different instances of OOB vulnerabilities often exhibit a wide range of capabilities.
2. Kernel exploitation is usually multi-interaction in nature (i.e., an exploit involves multiple system calls), which allows the exploit construction process to be modular.

### How KOOBE Works

- **Capability-oriented fuzzing**: KOOBE introduces a novel capability-oriented fuzzing approach aimed at uncovering hidden capabilities. Rather than blindly generating inputs, this kind of fuzzing purposefully seeks inputs that can expose specific vulnerability capabilities.
- **Capability combination**: KOOBE also provides a mechanism to combine different capabilities to increase the likelihood of successfully exploiting a vulnerability. In this way, even vulnerabilities that seem difficult to exploit may still yield an effective attack path.

### Experimental Results

During the evaluation, KOOBE performed a comprehensive analysis of 17 recent Linux kernel OOB vulnerabilities, of which only 5 had public exploit code. KOOBE successfully generated candidate exploit strategies for 11 vulnerabilities, including 5 that had not yet been assigned CVE numbers. Starting from these strategies, the researchers were able to build fully working exploits for all 11 vulnerabilities.

### Significance

This work is significant for improving the security of operating system kernels. It not only helps discover and assess the impact of OOB vulnerabilities, but also facilitates faster fixes for these issues, thereby reducing potential security threats. In addition, KOOBE's techniques and methodology can serve as a reference for other types of security research.


# A Motivating Example and the Automation Idea

## CVE-2018-5703
```c
1. struct Type1 { …; };
2. struct Type2 { Type1 sk; uint64_t option; …; };
3. struct Type3 { int (*ptr)(); …; };
4. struct Type4 { uint64_t state; Type3 *sk; …; };
5. struct Type5 { atomic_t refcnt; …; };

6. Type2 gsock = { …, .option = 0x08080000000000, };
7. Type1 * vul = NULL; Type3 * tgt = NULL;

8. void sys_socket() //sizeof(Type1) == sizeof(Type3)
9. vul = kmalloc(sizeof(Type1))

10. void sys_accept()
11. vul = (Type2*)vul; //type confusion
12. vul->option = gsock.option; //Vulnerability Point

13. void sys_setsockopt(val) //not invoked in given PoC
14. if (val == -1) return;
15. gsock.option = val;

16. void sys_create_tgt()
17. tgt = kmalloc(sizeof(Type3));
18. tgt->ptr = NULL; //init ptr

19. void sys_deref() { if (tgt->ptr) tgt->ptr(); }
```


1. Vulnerability overview:
   - The vulnerability point is at line 12, where a type confusion causes an out-of-bounds write to the original object (OOB write).
   - Two objects are involved: the vulnerable object (vul) and the target object (tgt).
   - The type confusion at line 11 leads to the out-of-bounds write at line 12.

2. Vulnerability characteristics:
   - At first glance, it seems that only the fixed value 0x08080000000000 can be written.
   - In reality, if sys_setsockopt is called first, the overwritten content is controllable.

3. Exploitation process (figure):
   - Use heap spraying (heap feng shui) techniques to manipulate the heap layout.
   - Create the target object so that it is adjacent to the vulnerable object.
   - Use the vulnerability to modify the pointer of the target object.
   - Trigger the pointer dereference to hijack the control flow.


```c
1. for (i = 0; i < N; i++)
2. sys_create_tgt(); // cache exhaustion
3. sys_socket(); // vuln obj
4. sys_create_tgt(); // target obj
5. sys_setsockopt(0xdeadbeef);
6. sys_accept(); // tgt->ptr = 0xdeadbeef
7. sys_deref();
```
Key point:
   - The original PoC lacks the sys_setsockopt call, which limits the exploitation value of the vulnerability.


![Image](/images/csdn/2024/csdn-142316865/image-001-b3acb2c802.png)

## Analyzing Capabilities (Exploitability)
For most vulnerabilities discovered through fuzzing, the corresponding PoC can usually corrupt some data, but does not necessarily lead to an exploitable state. For example, a PoC derived from random-mutation-based fuzzing might overwrite a pointer in the target object with some random value, resulting in a non-exploitable page fault, or corrupt some system data that causes a crash.

To assess its exploitability, security analysts usually need to examine the logic of the vulnerable code, then carefully adjust the system call arguments, insert additional system calls into the PoC (as described in the example), or even trigger the overwrite repeatedly.

## Heap Feng Shui
The current generation of Linux kernel heap allocators organizes dynamically allocated memory according to its size. Objects of the same size are managed by a dedicated cache (also called a slab), which reserves one or more pages from the system and pre-splits them into chunks of the same size for efficiency. For example, the Type1 or Type3 objects in Figure 1a are always allocated from the same cache because they have the same size. Whenever a cache is exhausted, it acquires new pages and splits them into chunks with consecutive addresses. Most importantly, these new chunks are allocated in order (from low addresses to high addresses) (e.g., via kmalloc()). This process is illustrated in the heap feng shui step in Figure 2. By exploiting this knowledge, we can exhaust the current cache (lines 1 and 2 in Figure 1b) to ensure that new pages will be requested in subsequent allocations, so that the vulnerable object and the target object managed by the same cache are allocated adjacently (lines 3 and 4 in Figure 1b, respectively). Note that it is not necessary to use the vulnerable object or the target object to exhaust the cache; in fact, several general objects of different sizes (e.g., msgbuf) have been found for this purpose [41].

In general, each system call may create multiple objects at once, which can complicate heap feng shui. However, given that the heap allocator is deterministic, and given the fact that an attacker can set up the heap layout in advance through a series of system calls, it is almost always possible to arrange memory to facilitate the exploitation of an out-of-bounds write vulnerability (e.g., making the vulnerable object and the target object adjacent).

## Selecting an Exploitation Target

After summarizing the capabilities and the pre-arranged memory layout, we need to carefully select a target object whose critical fields can be overwritten with the desired payload. In general, we can classify critical fields into (function/data) pointers and non-pointers.  

(i) **Function pointers** (e.g., Type3 in Figure 1a) are the most ideal, because controlling their values can lead to control-flow hijacking immediately after they are dereferenced.  

(ii) **Data pointers** can be used to construct an arbitrary write primitive (if they point to a structure that will be written to later), or can still be used for arbitrary code execution (if they point to another structure containing a function pointer, e.g., Type4). It is worth noting that heap metadata is a special target object, which contains a data pointer to the next free object in the cache [1].  

(iii) **Non-pointer fields** need to be evaluated case by case. For example, in Linux, the `uid` in `struct cred` is a commonly targeted special field that controls the user ID. If an attacker can overwrite the `uid` of their own process to 0, they can escalate the process's privileges to root. Another less common example is the reference counter widely used in Linux kernel objects (e.g., the first field of Type5 in Figure 1a). If the reference counter can be forcibly overwritten (e.g., to 0), the target object will be freed prematurely, leading to a UAF (Use-After-Free) vulnerability [4]. (In the Linux kernel, many objects use reference counters to manage their lifecycles. A reference counter (refcnt) is an atomic variable that tracks how many references point to the object. When the reference counter drops to 0, it means no references point to the object, which usually triggers the object's release.) Attackers can then leverage well-studied UAF-based exploitation techniques [57, 58].

As shown in Figure 1b, we choose Type3 as the target object because it has the same size as the vulnerable object (making heap feng shui easier) and has a function pointer in its first 8 bytes. This matches the capability, i.e., a total of 8 controllable bytes can be written past the vulnerable object. On the other hand, Type4 is not suitable for exploitation because its critical field (i.e., the data pointer `sk`) is not at the beginning.

When the capability of a specific out-of-bounds write vulnerability is limited, it is very important to collect a variety of objects containing critical fields. For example, CVE-2016-6187 shown in Figure 3a can only overflow one byte of zero, which is not enough to construct a pointer. However, it makes sense to choose a target object whose first field is a reference counter (e.g., Type5 in Figure 1a). This is because overwriting the least significant byte of the reference counter with zero is equivalent to decreasing its value, eventually turning it into a UAF vulnerability. In fact, in the Linux kernel, there are more than 2,000 objects that can serve as suitable attack targets.

```c
void example1(size) {
    vul = kmalloc(size);
    vul[size] = '\0';
}
// (a) CVE-2016-6187

void example2(i) {
    vul = (char*)kmalloc(sizeof(TYPE));
    // 省略路径上的其他越界点
    vul[i/8] |= 1 << (i & 0x7); // 设置1位
}
// (b) CVE-2017-7184
```

The above are two simplified CVEs. The one on the left allows overflowing a single zero byte, while the one on the right can only set a single bit at a controllable offset.


## Synthesizing the Exploit


Finally, based on the target object we selected earlier, we need to adjust the PoC (proof of concept) accordingly. Usually, the target object is known in advance (because the Linux kernel is open source). Specifically, we need to know how to allocate each object and how to trigger the dereference of the corresponding pointer. From there, we can combine this knowledge to synthesize a complete exploit.

Next comes bypassing advanced defenses and achieving arbitrary code execution. Modern defense measures usually include KASLR (Kernel Address Space Layout Randomization), SMEP (Supervisor Mode Execution Prevention), and SMAP (Supervisor Mode Access Prevention). Although these defenses make attacks more complicated, they do not necessarily stop them. We briefly outline some common strategies for bypassing these defenses below:
- **Bypassing KASLR**: In practice, a separate information disclosure vulnerability is usually used; alternatively, recent CPU side-channel attacks such as Meltdown [36], Spectre [34], RIDL [54], and ZombieLand [46] can all achieve this goal.
- **Bypassing SMEP**: One can simply direct the control flow into the kernel address space (ROP/JOP), which is not a big obstacle (no need to execute code in user space).
- **Bypassing SMAP**: A corrupted data pointer can be pointed at the kernel's physical map region (physmap), where a controllable object is forged using physmap spraying techniques [33, 58].
- **Finally, turning IP hijacking into arbitrary code execution and privilege escalation**: Recent research [56] can automate this process even with SMEP and SMAP enabled.


# Design


## Vulnerability Analysis

Given a PoC (proof of concept), our system first attempts to discover all vulnerability points (i.e., out-of-bounds access sites) and identify the corresponding vulnerable objects (see Figure 10 in Appendix B for details). Unfortunately, KASAN [5] alone cannot provide complete vulnerability points or accurate reports of vulnerable objects. KASAN is known for potentially missing out-of-bounds accesses because it relies on shadow memory and red zones [51], and is ineffective for out-of-bounds accesses that do not overflow into a red zone (e.g., cases that only overwrite a nearby object). In fact, we found that in some cases KASAN can only report one of multiple out-of-bounds accesses. Moreover, KASAN cannot accurately locate the vulnerable object, because it only reports the object closest to the accessed red zone.

To this end, when executing the PoC, in addition to basic KASAN, we also perform symbolic tracing to monitor more detailed memory operations (an offline step for each PoC), such as `kmalloc()` and individual memory accesses. Specifically, our system uses symbolic tracing to track each object, assigning it a unique symbolic value when it is created. Therefore, for each memory access, if it contains a symbolic expression, we can directly extract the target object. Furthermore, by querying the possible range of a pointer's symbolic expression, we can detect a potential overflow even if the given PoC does not trigger one. In the motivating example, if we assign a symbolic value to the vulnerable object returned from the `kmalloc()` function (line 9), we obtain the following symbolic expression for the pointer at line 12: `vul + offsetof(Type2, option)`, where `vul` is the symbolic value we assigned. By analyzing the symbolic expression of the pointer in Figure 3b (i.e., `vul + i/8`, where both `vul` and `i` are symbolic values — `i` is passed in from a system call argument), we can conclude that this must be an out-of-bounds vulnerability point, because the offset can be larger than the size of the vulnerable object, since `i` has no constraints (even though the PoC does not use a sufficiently large `i`).


## Capability Summarization

## Related Definition 1: OOB Write Representation
A bit abstract
### Symbolic Expression Set \( E \)
- **Symbolic expression set \( E \)**: This is the set of all possible expressions supported by the symbolic execution engine. These expressions can be combinations of variables, constants, operators, etc. For example, `x + 5`, `y * 2`, and `z - 1` are all symbolic expressions.

### Path Set \( P \)
- **Path set \( P \)**: This is the set of all possible execution paths in the program. Each path represents one way the program can execute. For example, a simple if-else statement produces two paths: one where the if condition is true, and another where the if condition is false.

### Vulnerability Point Set \( N_p \) on Path \( p \)
- **Vulnerability point set \( N_p \) on path \( p \)**: This is the set of all possible vulnerability points on a specific path \( p \). A vulnerability point is a location in the program that may cause a security problem, such as a buffer overflow or an out-of-bounds write.

### Example

Suppose we have a simple C program:

```c
void vulnerable_function(int x) {
    int buffer[10];
    if (x > 0) {
        buffer[x] = 1;  // 可能的漏洞点
    } else {
        buffer[0] = 1;  // 不是漏洞点
    }
}
```

In this program, we can have two paths:

1. **Path 1**: `x > 0`
   - On this path, `buffer[x] = 1` is a possible vulnerability point, because if `x` is greater than 10, an out-of-bounds write will occur.
   - Therefore, the vulnerability point set \( N_{p_1} \) on path 1 contains one element: `buffer[x] = 1`.

2. **Path 2**: `x <= 0`
   - On this path, `buffer[0] = 1` is not a vulnerability point, because `buffer[0]` is legitimate.
   - Therefore, the vulnerability point set \( N_{p_2} \) on path 2 is empty.

### Mathematical Notation

- **Vulnerability point set \( N_p \) on path \( p \)**: This is the set of all possible vulnerability points on path \( p \).
- **\( i \in N_p \)**: indicates that \( i \) is a vulnerability point on path \( p \).
- **\( off, len, val \in E \)**: indicates that the offset \( off \), length \( len \), and value \( val \) are all symbolic expressions belonging to the set \( E \).

### Plain Explanation

- **Path \( p \)**: simply one way the program executes.
- **Vulnerability point \( i \)**: a specific location on path \( p \) that may cause a security problem.
- **Offset \( off \)**: the offset from the starting address of the vulnerable object.
- **Length \( len \)**: the number of bytes that can be written.
- **Value \( val \)**: the value being written.

### Example Explanation

Suppose we have a path \( p \) with two vulnerability points \( i_1 \) and \( i_2 \) on it:

- **Vulnerability point \( i_1 \)**: `buffer[x] = 1`, where `x` is a symbolic expression.
  - **Offset \( off_{p_1} \)**: `x` (relative to the starting address of `buffer`)
  - **Length \( len_{p_1} \)**: 1 (writes 1 byte)
  - **Value \( val_{p_1} \)**: 1 (the value written)

- **Vulnerability point \( i_2 \)**: `buffer[y] = 2`, where `y` is a symbolic expression.
  - **Offset \( off_{p_2} \)**: `y` (relative to the starting address of `buffer`)
  - **Length \( len_{p_2} \)**: 1 (writes 1 byte)
  - **Value \( val_{p_2} \)**: 2 (the value written)

Therefore, the OOB write set \( T_p \) on path \( p \) can be expressed as:

\[ T_p = \{(x, 1, 1), (y, 1, 2)\} \]
## Related Definition 2: Capability Extraction

The capability of a path \( p \) is expressed as  Cp = {sizep,Tp, f(p) | sizep ∈ E}, where:
- **\( size_p \)**: represents the size of the vulnerable object.
- **\( T_p \)**: represents the OOB write set on path \( p \).
- **\( f(p) \)**: represents the set of path constraints collected while executing path \( p \).
- **\( size_p \in E \)**: indicates that \( size_p \) is an element of the symbolic expression set \( E \).

### Detailed Explanation

1. **Size of the vulnerable object \( size_p \)**
   - **Definition**: \( size_p \) represents the size of the vulnerable object on path \( p \).
   - **Importance**: In some cases, the size of the vulnerable object is variable, which affects the possibility and scope of the OOB write. For example, if the object's size can be controlled by user input, this increases the search space for finding a suitable target object.
   - **Symbolic expression**: \( size_p \) is a symbolic expression belonging to the set \( E \).

2. **OOB write set \( T_p \)**
   - **Definition**: \( T_p \) represents the set of all OOB writes on path \( p \).
   - **Structure**: Each OOB write can be represented as a triple \( (off, len, val) \), where:
     - **\( off \)**: represents the offset relative to the starting address of the vulnerable object.
     - **\( len \)**: represents the number of bytes that can be written.
     - **\( val \)**: represents the value written.
   - **Example**: Suppose there are two OOB write points on path \( p \), namely \( (10, 2, 0x41) \) and \( (15, 1, 0x42) \); then \( T_p = \{(10, 2, 0x41), (15, 1, 0x42)\} \).

3. **Path constraints \( f(p) \)**
   - **Definition**: \( f(p) \) represents the set of path constraints collected while executing path \( p \).
   - **Importance**: Path constraints limit the actual possibilities of an OOB write. For example, a value must satisfy a specific condition to reach a certain line of code.
   - **Example**: Suppose there is a conditional check `val != -1` on path \( p \); then \( f(p) \) contains this constraint.

### Example Explanation

Suppose there is a C program containing a vulnerable function:

```c
void vulnerable_function(char *data, int len, int obj_size) {
    char *buffer = malloc(obj_size);
    if (obj_size > 0 && obj_size < 20) {
        memcpy(buffer, data, len);
    }
    free(buffer);
}
```

In this example, we can discover two paths through symbolic execution:

1. **Path 1**: `len = 16`, `obj_size = 15`
   - **Path constraints**: `obj_size > 0 && obj_size < 20`
   - **Vulnerability point**: `memcpy(buffer, data, 16)`, which writes 1 byte past `buffer`.
   - **OOB write set**: \( T_{p_1} = \{(15, 1, data[15])\} \)
   - **Capability**: \( C_{p_1} = \{15, T_{p_1}, \{obj_size > 0, obj_size < 20\}\} \)

2. **Path 2**: `len = 18`, `obj_size = 15`
   - **Path constraints**: `obj_size > 0 && obj_size < 20`
   - **Vulnerability point**: `memcpy(buffer, data, 18)`, which writes 3 bytes past `buffer`.
   - **OOB write set**: \( T_{p_2} = \{(15, 3, data[15])\} \)
   - **Capability**: \( C_{p_2} = \{15, T_{p_2}, \{obj_size > 0, obj_size < 20\}\} \)

### Why Path Constraints Matter

Path constraints limit the actual possibilities of an OOB write. For example, in the example above, `obj_size` must be between 1 and 19, otherwise `malloc` and `memcpy` will not be executed. Therefore, the path constraints \( f(p) \) are an important factor in determining whether an OOB write is feasible.



- **Size of the vulnerable object \( size_p \)**: represents the size of the object, which may affect the scope of the OOB write.
- **OOB write set \( T_p \)**: represents the set of all OOB writes on path \( p \), where each write consists of an offset, a length, and a value.
- **Path constraints \( f(p) \)**: represent the path constraints collected while executing path \( p \), which limit the actual possibilities of the OOB write.


## Related Definition 3: Capability Comparison


- **Definition**: For all \( e1, e2 \in E \), if \( e1 \) is identical to \( e2 \), or \( e1 \) is a constant whose value can be taken by \( e2 \), then \( e1 \leq e2 \).
- **Explanation**: This defines a comparison relation between symbolic expressions. If two expressions are identical, or one of them is a constant whose value can be taken by the other expression, the former is considered less than or equal to the latter.

#### OOB Write Set Comparison
- **Definition**: For all \( p1, p2 \in P \), if \( off_{p1_i} \leq off_{p2_i} \) and \( len_{p1_i} \leq len_{p2_i} \) and \( val_{p1_i} \leq val_{p2_i} \), then \( T_{p1_i} \leq T_{p2_i} \).
- **Explanation**: This defines a comparison relation between two OOB write sets. If the offset, length, and value of one OOB write are all less than or equal to the corresponding values of another OOB write, the former is considered less than or equal to the latter.

#### Capability Comparison
- **Definition**: For all \( p1, p2 \in P \), if \( size_{p1} \leq size_{p2} \) and for all \( i \in N_{p1} \), \( T_{p1_i} \leq T_{p2_i} \), then \( C_{p1} \leq C_{p2} \).
- **Explanation**: This defines a comparison relation between the capabilities of two paths. If the size of the vulnerable object on one path is less than or equal to the size of the vulnerable object on another path, and all OOB writes on that path are less than or equal to the corresponding OOB writes on the other path, then the capability of the former is considered less than or equal to the capability of the latter.

### Observation
Directly comparing symbolic expressions can be tricky, because there are intrinsic relationships between them, especially when combined with path constraints. Therefore, we conservatively consider the former to be equal to or inferior to the latter only when the two expressions are identical, or the former is a constant whose value can be taken by the latter. Based on this, we can define a partial order over OOB writes and capabilities by comparing each element. As can be seen from the example above, the second capability is superior, because \( C_{orig} \leq C_{comp} \).


### 1. Intrinsic Relationships Between Symbolic Expressions

The intrinsic relationships between symbolic expressions refer to certain dependencies, equivalences, or logical connections between these expressions, which mean they cannot simply be treated independently.

#### Example 1: Dependency
Suppose we have two symbolic expressions:
- \( e1 = x + 5 \)
- \( e2 = y + 5 \)

If \( y = x + 10 \), then \( e2 = (x + 10) + 5 = x + 15 \). In this case, although \( e1 \) and \( e2 \) are similar in form, their values are different because \( y \) depends on \( x \).

### 2. The Influence of Path Constraints

Path constraints are conditions collected during program execution that determine the program's execution path. Path constraints restrict the values that symbolic expressions can take, making the relationships between symbolic expressions more complex.

#### Example 2: Path Constraints
Suppose we have a program fragment:

```c
int x, y;

if (x > 0) {
    y = x + 5;
} else {
    y = x - 5;
}
```

In this example, the path constraint for path 1 is \( x > 0 \), and the path constraint for path 2 is \( x \leq 0 \).

### 3. Combining Symbolic Expressions with Path Constraints

When symbolic expressions are combined with path constraints, things become more complex. Path constraints restrict the values that symbolic expressions can take, so these constraints must be considered when comparing symbolic expressions.

#### Example 3: Symbolic Expressions and Path Constraints
Suppose we have two symbolic expressions:
- \( e1 = x + 5 \)
- \( e2 = y + 5 \)

And we have path constraints:
- Path 1 constraint: \( x > 0 \)
- Path 2 constraint: \( y > 10 \)

In this case, even though \( e1 \) and \( e2 \) are similar in form, their value ranges are different. On path 1, \( x \) can take any value greater than 0, while on path 2, \( y \) can take any value greater than 10. Therefore, directly comparing \( e1 \) and \( e2 \) is not straightforward; the path constraints must be considered.

#### Concrete Example

Suppose we have a more complex C program containing multiple conditional branches and path constraints:

```c
int x, y, z;

if (x > 10) {
    y = x + 5;
    if (y > 15) {
        z = y + 10;
    } else {
        z = y - 10;
    }
} else {
    y = x - 5;
    if (y < -10) {
        z = y + 10;
    } else {
        z = y - 10;
    }
}
```

#### Symbolic Expressions
- **Path 1**: `x > 10` and `y > 15`
  - **Symbolic expressions**:
    - `y = x + 5`
    - `z = y + 10`
- **Path 2**: `x > 10` and `y <= 15`
  - **Symbolic expressions**:
    - `y = x + 5`
    - `z = y - 10`
- **Path 3**: `x <= 10` and `y < -10`
  - **Symbolic expressions**:
    - `y = x - 5`
    - `z = y + 10`
- **Path 4**: `x <= 10` and `y >= -10`
  - **Symbolic expressions**:
    - `y = x - 5`
    - `z = y - 10`

#### Path Constraints
- **Path 1 constraints**: `x > 10` and `y > 15`
- **Path 2 constraints**: `x > 10` and `y <= 15`
- **Path 3 constraints**: `x <= 10` and `y < -10`
- **Path 4 constraints**: `x <= 10` and `y >= -10`

#### Intrinsic Relationships
- **Path 1**: When `x > 10` and `y > 15`, `y = x + 5` and `z = (x + 5) + 10 = x + 15`. Here, the values of `y` and `z` depend on the value of `x`, and `x` must be greater than 10 and `y` must be greater than 15.
- **Path 2**: When `x > 10` and `y <= 15`, `y = x + 5` and `z = (x + 5) - 10 = x - 5`. Here, the values of `y` and `z` depend on the value of `x`, and `x` must be greater than 10 and `y` must be less than or equal to 15.
- **Path 3**: When `x <= 10` and `y < -10`, `y = x - 5` and `z = (x - 5) + 10 = x + 5`. Here, the values of `y` and `z` depend on the value of `x`, and `x` must be less than or equal to 10 and `y` must be less than -10.
- **Path 4**: When `x <= 10` and `y >= -10`, `y = x - 5` and `z = (x - 5) - 10 = x - 15`. Here, the values of `y` and `z` depend on the value of `x`, and `x` must be less than or equal to 10 and `y` must be greater than or equal to -10.


### Capability Generation


#### Classifying Vulnerability Points
- **Classification**: We usually classify the vulnerability points identified in the previous step into two categories: function calls and memory access instructions.
  - **Function calls**: If the OOB access is triggered by a memory copy function (such as `memcpy()`), the corresponding vulnerability point is the instruction that calls that function.
  - **Memory access instructions**: Otherwise, the instruction that causes the OOB write is directly regarded as the vulnerability point.

#### Modeling memory copy functions
- **Purpose**: Modeling memory copy functions can simplify capability extraction (because it avoids the loop analysis that we will describe in detail how to handle).
- **Method**:
  - **Offset**: Through symbolic tracing, the write offset can be extracted from the first argument (destination address) of `memcpy()`.
  - **Value**: The value written can be extracted from the second argument (source address) of `memcpy()`.
  - **Length**: The write length can be extracted from the third argument of `memcpy()`.


### Example Explanation

Suppose we have a program containing a vulnerable function:

```c
void vulnerable_function(char *data, int len, int obj_size) {
    char *buffer = malloc(obj_size);
    if (obj_size > 0 && obj_size < 20) {
        memcpy(buffer, data, len);
    }
    free(buffer);
}
```

In this example, we can discover two paths through symbolic execution:

1. **Path 1**: `len = 16`, `obj_size = 15`
   - **Path constraints**: `obj_size > 0 && obj_size < 20`
   - **Vulnerability point**: `memcpy(buffer, data, 16)`, which writes 1 byte past `buffer`.
   - **OOB write set**: \( T_{p1} = \{(15, 1, data[15])\} \)
   - **Capability**: \( C_{p1} = \{15, T_{p1}, \{obj_size > 0, obj_size < 20\}\} \)

2. **Path 2**: `len = 18`, `obj_size = 15`
   - **Path constraints**: `obj_size > 0 && obj_size < 20`
   - **Vulnerability point**: `memcpy(buffer, data, 18)`, which writes 3 bytes past `buffer`.
   - **OOB write set**: \( T_{p2} = \{(15, 3, data[15])\} \)
   - **Capability**: \( C_{p2} = \{15, T_{p2}, \{obj_size > 0, obj_size < 20\}\} \)

### Comparing Capabilities
- **Offset**: `off_{p1} = 15` and `off_{p2} = 15` are equal.
- **Length**: `len_{p1} = 1` and `len_{p2} = 3`, so `len_{p1} < len_{p2}`.
- **Value**: `val_{p1} = data[15]` and `val_{p2} = data[15]` are equal.

Therefore, by definition, \( C_{p1} \leq C_{p2} \), i.e., the capability of path 2 is superior to the capability of path 1.

![Image](/images/csdn/2024/csdn-142316865/image-002-7cb14841be.png)





## Capability Exploration

Usually, a vulnerability leads to different vulnerability points on different paths, each of which may exhibit a unique capability. (Exploiting the vulnerability may cause new vulnerability points to appear on the different paths where the vulnerability resides.) Moreover, even for the same vulnerability point, different paths and associated path constraints may lead to different capabilities, as shown in Figure 1a. Unfortunately, a given PoC (proof of concept) usually covers only one path, which may limit our understanding of the vulnerability's full capabilities. Therefore, as shown in Figure 4, if our system fails to find a solution using the discovered capabilities (fails to locate a suitable target object to attack), it explores new PoCs that either extend existing capabilities or discover new ones, and then repeats the process of capability summarization and exploitability evaluation until it succeeds or reaches a preset timeout. To this end, our system adopts a novel capability-guided fuzzing solution to explore additional capabilities.

#### Capability-Guided Fuzzing
Fuzzing is a natural solution for exploring different exploitable states. However, existing kernel fuzzers (such as Syzkaller) are primarily coverage-guided and are not effective at exploring OOB (out-of-bounds) capabilities. This is because maximizing branch coverage is only a very loose approximation of discovering more OOB capabilities — it often prioritizes test programs that achieve new coverage (which may not even trigger an OOB) and is insensitive to the OOB capabilities actually discovered. This motivated us to design a capability-guided fuzzing strategy combined with coverage guidance. Given a PoC and its corresponding OOB capability, we mutate it and collect capability feedback (whenever an OOB is triggered) as well as coverage feedback. Finally, we feed the seeds with new capabilities into the symbolic tracing engine for further summarization. Compared with an existing capability \( C_{p1} \), a newly extracted capability \( C_{p2} \) is considered a new capability if \( C_{p2} \leq C_{p1} \) is false.

Specifically, whenever a new test program is executed, we collect the concrete values of the OOB write set at runtime as capability feedback (e.g., how many bytes were written and what values were written). Note that unlike the heavyweight capability summarization using symbolic tracing, we use lightweight dynamic instrumentation in the fuzzing component to collect the OOB write set (more details about the instrumentation are described at the beginning of Section 5). However, the trade-off is that if we determine whether a new capability has been discovered only by comparing concrete values, some test cases may be duplicates, because we could later generalize them through capability summarization. For example, if we know that the overwritten value can be arbitrary, keeping test cases that differ only in the overwritten value during fuzzing is redundant. To address this, KOOBE performs capability summarization each time a new vulnerability point is discovered, and then provides the fuzzing engine with the range of values in the OOB write set to filter test cases. Thus, it can detect "duplicate" inputs by checking concrete values against the ranges collected through symbolic tracing, rather than comparing symbolic values. Note that, as shown in Figure 4, vulnerability analysis (see Section 4.1) is always performed before capability summarization to avoid missing any OOB sites that KASAN fails to detect.

In our design, we maintain a balance among the test programs in the corpus. Since it is usually easier to increase coverage than to discover new capabilities, the distribution of new test programs in the corpus may be heavily biased toward those that increase coverage. We change the seed selection strategy by maintaining two queues: one for programs that increase coverage and another for programs that extend capabilities, and we select seeds from both queues with equal probability. This configuration produced good results in our experiments (to be shown in Section 6.4); we leave the exploration of different probability configurations to future work (see Section 7 for more discussion).


## Exploitability Evaluation
Given the capabilities obtained from the previous steps, our system now attempts to search for one or more suitable target objects in the Linux kernel. If a match is found, a solution is produced (see Figure 13 in Appendix B for a concrete example).

We first introduce the concept of target constraints, which represent the conditions under which a target object can be overwritten to lead to a potential exploit. They describe which fields need to be overwritten (e.g., function pointers, data pointers, reference counters, or any custom data) and the expected value ranges of these fields. For example, a pointer must point to a valid user-space or kernel-space address. In addition, due to the requirements of heap feng shui, we require the target object to be the same size as the vulnerable object. However, this requirement can be removed, because advanced heap feng shui strategies can place the target object adjacent to the vulnerable object even when their sizes differ. Nevertheless, doing so is less stable, so we prefer to choose target objects of the same size.

Then, we overlay the target constraints on top of the capabilities we derived earlier and pass them to the solver to look for a solution. If no solution is found, we move on to the next object.

Figure 5a depicts a general model in which one or more memory accesses overwrite a target object adjacent to the vulnerable object, assuming heap feng shui can manipulate the heap layout as needed (we take the case with only one out-of-bounds write as an example, but this can be generalized to multiple out-of-bounds writes).
![Image](/images/csdn/2024/csdn-142316865/image-003-47341aced2.png)

More specifically, our system constructs a memory object M to model the memory regions of the vulnerable object and the target object, allowing its contents to be updated with symbolic indices, values, and lengths (see Section 5 for details). After initializing the memory object M with the symbolic data/indices/offsets provided by the capability, we can evaluate whether a candidate object is suitable by adding target constraints on the memory object M and checking satisfiability relative to the path constraints retrieved from capability summarization.

![Image](/images/csdn/2024/csdn-142316865/image-004-ddc6b04e1a.png)

Figure 5b illustrates the process in the motivating example, where two target objects (Type3 and Type4) are considered. The first line simply states that the size of the vulnerable object must match the size of the target object. The second and third lines, concerning the out-of-bounds offset and the out-of-bounds length (both constants), are used to update the memory object, and the fourth line represents the out-of-bounds value (an 8-byte symbolic value). Finally, the last line includes the path constraints (collected as part of the capability) and the value range required by the payload (as part of the target constraints). In this case, a Type3 target object expects its first field (an 8-byte function pointer from index 0 to 7) to be overwritten with a valid user-space or kernel-space address, which is indeed satisfiable. On the other hand, due to the limited out-of-bounds offset and out-of-bounds length, the second field of a Type4 target object cannot be overwritten.



**Capability combination**. When a single use of a capability — which may already contain multiple out-of-bounds accesses — cannot satisfy the requirements of a given target object, it does not necessarily mean the capability is useless, because the capability may only be able to modify part of the target object at a time (e.g., a single bit). Therefore, if we reuse the same capability (i.e., re-trigger the same out-of-bounds write path) to operate on the remaining parts, we can achieve the desired value. For example, CVE-2017-7184 shown in Figure 3b can modify a null pointer to an arbitrary value even though it only sets one bit at a time. In cases where the allocation of the vulnerable object and the overflow occur in different system calls, we can trigger the out-of-bounds write from the same vulnerable object multiple times by invoking the corresponding system call sequence repeatedly. Moreover, beyond reusing the same capability, some vulnerabilities require combining different capabilities to be exploited (e.g., capabilities with different out-of-bounds values). To this end, we propose an efficient greedy algorithm to evaluate exploitability given different capabilities, as shown in Appendix A.
![Image](/images/csdn/2024/csdn-142316865/image-005-fbbd0339c1.png)

Instead of brute-forcing every possible capability combination, the key idea is to use only one capability per iteration to approach the desired target field value until the result no longer changes. Then, we check whether the final result is satisfactory, i.e., whether a solution has been obtained. Therefore, depending on the type of the target field (e.g., data or function pointer), we define a corresponding distance function as the objective function, guiding us to select the best capability in each iteration to minimize the distance. Note that each selection writes its result back into the memory object, so that the next iteration can continue to reduce the distance. Table 1 describes the distance functions for the three target field types mentioned in Section 3. For function pointer and non-pointer types, a payload is usually provided (e.g., a redirection address — i.e., just an address to write), while the data pointer type requires the modified value to lie within a valid memory region (whether kernel space or user space). Therefore, the distance functions for these corresponding target types have the following two properties:

1. Return zero if the condition is satisfied: For example, the distance function for the data pointer type returns zero only when the value lies within a valid range (i.e., \[MIN_POINTER, MAX_POINTER\]); otherwise, it returns a positive distance.
2. Differentiability: It enables our greedy algorithm to distinguish which capability can help us get closer to the desired payload.

Note that, given the two properties above, it is not difficult to derive distance functions for target objects containing multiple critical fields. For example, the distance function for the conjunction of two target fields is the sum of the individual distances, while the distance function for a disjunction is the minimum of the two individual distances. Generalizing to joint distance functions for more than two target fields is also straightforward.
![Image](/images/csdn/2024/csdn-142316865/image-006-86672ba3a3.png)
## Exploit Primitive Synthesis

Once our system successfully finds a solution — which is essentially the concrete system call arguments previously marked as symbolic — the next step is obviously to perform heap feng shui to construct the layout assumed in the previous step and trigger the corrupted field (e.g., a function pointer) to be dereferenced. For heap feng shui, we encoded several well-known strategies, as described in Section 3, which are sufficient for all the cases we encountered.
Specifically, we perform heap spraying through three different system calls — add_key(), msgsnd(), and sendmsg() — adopt the technique introduced in [58] to achieve cache exhaustion, and insert the allocation and dereference functions of the selected target at appropriate positions (see Figure 2 in Appendix B for details). We manually collected the target objects used in all public exploits we found online and built a database specifying how they are used (as shown in Figure 12). In addition, we selectively sampled some promising objects in our evaluation to assist this step (see Section 5).

![Image](/images/csdn/2024/csdn-142316865/image-007-245627d8c9.png)

As mentioned earlier, since our goal is to achieve an IP hijacking primitive rather than an end-to-end solution for arbitrary code execution (which may involve ROP/JOP to bypass SMEP), we explicitly exclude these modern defenses (e.g., KASLR, SMEP). However, in special cases where we need to forge a controllable kernel object, we use physmap spray [33] to avoid violating SMAP (see Figure 14 in Appendix B for details).
![Image](/images/csdn/2024/csdn-142316865/image-008-d59ad0f77d.png)
The logic of the above paragraph is really hard to understand. Here is an improved version:

1. **Goal**: The attacker's goal is to achieve IP (instruction pointer) hijacking. This means the attacker wants to control the program's execution flow rather than directly achieve arbitrary code execution. IP hijacking can be used as part of a more complex attack.

2. **Modern defenses**:
   - **KASLR (Kernel Address Space Layout Randomization)**: By randomizing the kernel's location in memory, it increases the difficulty for an attacker to guess kernel addresses.
   - **SMEP (Supervisor Mode Execution Prevention)**: Prevents the kernel from executing code from user space, increasing the difficulty of attacks.
   - **SMAP (Supervisor Mode Access Prevention)**: Prevents the kernel from accessing user-space data.

3. **Not considering bypassing KASLR and SMEP**: Since the goal is only to achieve IP hijacking rather than a complete arbitrary code execution scheme, the text explicitly states that bypassing these defenses (KASLR and SMEP) is not considered.

4. **Special cases and the physmap spray technique**:
   - In certain special cases, the attacker needs to create a controllable kernel object.
   - **Physmap spray**: This is a technique in which the attacker fills the physical address space by allocating large amounts of memory. This allows attacker-controlled data to be placed into the kernel's physical address space in certain cases.
   - Using this technique avoids violating SMAP restrictions, because the attacker creates a controlled object inside the kernel in this way.

In summary, this passage describes a specific attack strategy focused on achieving IP hijacking, using the physmap spray technique to gain control over a kernel object without violating SMAP. Defenses such as KASLR and SMEP are considered out of scope because the goal is not to achieve arbitrary code execution.



# Implementation


We have implemented a prototype of the system on top of the popular kernel fuzzer Syzkaller, the binary symbolic execution framework S2E [21], and the binary analysis engine angr [50]. The system includes:

- 7,510 lines of C++ code for capability summarization and exploitability evaluation on S2E,
- 2,271 lines of Python code based on angr for vulnerability analysis,
- 1,106 lines of Go code for exploring divergent paths and synthesizing exploits.

---
**Dynamic instrumentation supporting capability-guided fuzzing**. In addition to using S2E for symbolic tracing and generating symbolic representations of capabilities, we also integrate S2E with Syzkaller through the QEMU provided by S2E, leveraging its powerful binary-level instrumentation to support capability-guided fuzzing (as described in Section 4.3). Moreover, through dynamic instrumentation, Syzkaller can inspect the kernel's internal state and perform non-crashing fuzzing. Specifically, since our initial seed (i.e., the given PoC) is already able to crash the system, we want the mutated programs to trigger the same crash. Therefore, if the system had to be restarted every time a test case is run, it would be extremely inefficient. To address this, we insert instrumentation code in the kernel to skip the instructions that cause OOB writes (while recording the operands of each OOB access to check whether they are new) and avoid any KASAN warnings, so as to keep the fuzzing session going. The downside is that this can lead to inconsistencies in the system state, producing potential false positives (i.e., falsely reporting new vulnerability points or new capabilities). However, our observation is that we only skip vulnerability points related to accesses to dynamically allocated heap objects. After each test program finishes executing, these heap objects are freed, so they do not interfere with future test program runs. Nevertheless, we can filter out programs that generate non-reproducible vulnerabilities by rerunning these programs in a standard Syzkaller environment. ("Reproducible" simply means both runs go through.)


---

**Supporting symbolic lengths**: In the key step of exploitability evaluation, we need to update the memory model `M[offset: offset+length-1] = value[0: length-1]` (see Section 4.4), where `offset`, `length`, and `value` may all be symbolic. However, symbolic lengths are usually very poorly supported in symbolic execution engines, unlike symbolic indices and values, which are easier to handle. Usually, we need to specify a concrete length for symbolic data [2, 7], so symbolic lengths cannot be used to update out-of-bounds (OOB) values in the memory object. Unfortunately, concretizing the length leads to an underestimation of the capability (in reality we should be able to write more or fewer bytes). This problem is mitigated when performing capability-guided fuzzing, because such fuzzing can generate PoCs (proofs of concept) that produce different concrete OOB lengths. However, relying on fuzzing to generate all possible concrete OOB lengths is impractical. In practice, we have multiple reasons to search for a solution over a range of OOB lengths, which is best supported when symbolic lengths can be handled: (1) We usually prefer solutions with the minimal OOB length to avoid corrupting system data (which could cause a crash). (2) We may need to constrain the OOB length according to the size requirement of the vulnerable object, especially when they are coupled (i.e., the overflow reaches not only the target object but also the next adjacent object).


```c
1. void loop(n) // n = 64
2. vul = (char*)kmalloc(32);
3. for (i = 0; i < n; i++)
4. vul[i] = 0; // OOB Point
```
Figure 6: An overflow example with a loop

Intuitively, our solution is no different from enumerating different possible OOB lengths, but we do it in a more efficient way that is compatible with existing memory models and solvers. Specifically, for a summarized OOB write operation `(off, len, val)` where all elements are symbolic and the concrete length is 10, our system updates the memory object `M` byte by byte as follows:
```python
for i in [0, 10]:
    M[ite(i < len, i + off, offsetdummy)] = val[i]
```
where `ite` denotes the if-then-else expression supported by KLEE and Z3 [10], and `offsetdummy` denotes the offset of a dummy byte we introduce to cancel the memory update of a specific byte. (offsetdummy is a dummy memory address that does not point to an actual memory region. By using offsetdummy, the program does not actually update any valid memory address. The purpose of this approach is to avoid unnecessary or incorrect writes to real memory when the condition is not satisfied (i.e., when i >= len).) Essentially, the solver can search for a feasible solution within a length range of 0 to 10 and update the memory model accordingly.

As we saw in the example, we conservatively search backward only from the concrete OOB length (0 to 10). This is because the byte values at larger indices cannot be predicted, whereas predicting at smaller indices is safe (if the length is smaller), because we have already seen them assigned and we know when the path will not change (by respecting the path constraints on the OOB length). Note that we rely on capability-guided fuzzing to find larger lengths. This assumption may fail when lower bytes are computed based on higher bytes (when the OOB value is symbolic), such as in encryption and compression contexts. However, we believe such cases are rare in the Linux kernel, and symbolic execution/tracing would already get stuck in the solver when encountering such procedures (it is quite hard to solve for the concrete values of the corresponding symbols that satisfy the requirements). In our experiments, we did not encounter any such cases; the assumption always held.

Symbolic lengths are usually very poorly supported in symbolic execution engines, unlike symbolic indices and values, which are easier to handle.


```c
1. void loop(int n) // n 是符号化的
2. {
3.     char *vul = (char *)malloc(32); // 分配 32 字节的内存
4.     for (int i = 0; i < n; i++)
5.     {
6.         vul[i] = 0; // OOB Point
7.     }
8.     free(vul);
9. }
```
- Path constraints generated: 0 <= i < n.
Each time i increases, the path constraint 0 <= i < n is re-evaluated.
- Each path constraint independently describes the condition for the program to reach a certain state.
Therefore, each value of n generates a new path, because each path constraint is unique.
- If n is symbolic, every possible value of n generates a new path, causing the number of paths to increase dramatically.
 Symbolic lengths introduce more complex constraints, making the constraint solver's solving process more complex and time-consuming. For example, the path constraint 0 <= i < n needs to be solved for every possible value of n.
---

**Capability extraction in loops**: As shown in lines 3-4 of Figure 6, the length of the overflowed data is determined by the input n, which has been symbolized. However, when loops are involved, existing symbolic execution techniques have a limitation — the symbolic value n is not propagated to the index i. This input-dependent loop problem is a common problem in symbolic execution that has not been fully solved. To address this, we borrow SAGE's approach, which uses some simple loop guard pattern matching rules to dynamically infer the formula for the index. We follow the same assumption that the induction variable (e.g., the index i) has a linear relationship with its guard condition. Since we only need to focus on the specific loops involving the vulnerability point, we decided to use Angr for static analysis (rather than the dynamic analysis suggested in the original paper). As mentioned in §4.2, the ability to handle loops is crucial for capability summarization.

```c
void process(char* input, int n) {
    char buffer[10];
    for (int i = 0; i < n; i++) {
        buffer[i] = input[i];
    }
}
```


In traditional symbolic execution, a symbolic value (such as `n`) usually does not directly affect the concrete value of the loop variable (such as `i`) after entering the loop. In this case, each loop iteration generates new path constraints, which are nested, causing the path constraints to grow dramatically. For loops, this can lead to path explosion, because each path constraint may produce a new path. The corresponding new paths can be thought of as trying each value of n one by one. In addition, the symbolic executor may not be able to precisely determine how `i` changes with each iteration, because although the logic of `i`'s change is clear (increment by 1 each time), its final value depends on the symbolic value `n`.

The rough process:
1. Loop unrolling
Limited unrolling: The symbolic executor may choose to unroll the loop a limited number of times, say 3 times; then regardless of the value of n, the loop will only execute 0 to 3 times, and each unrolling produces new path branches.
2. Path branching
Branch point: At the end of each loop iteration, the symbolic executor faces a branch point: continue the loop or exit the loop. For each possible value of n, this decision produces different paths:
Continue the loop: if i < n is true, proceed to the next iteration.
Exit the loop: if i >= n, exit the loop.
3. Symbolic constraints
Constraint generation: In each iteration, the symbolic executor generates or updates path constraints. For i < n, if n is symbolic, this constraint is added or updated in each iteration.
For example, after the first iteration, the constraint is i < n && i == 1.
After the second iteration, the constraint may be updated to i < n && i == 2.
4. Constraint solving
Solving and branching: The symbolic executor uses a constraint solver to decide whether there exists a value of n that makes the current path feasible. If feasible, it continues along this path; if not, it backtracks to the previous decision point to explore other paths.


**Solution - Loop Guard Pattern Matching**:

- **Dynamically inferring the index formula**: SAGE (and similar methods) solve this problem by observing the structure of the loop. They use pattern matching to identify common loop patterns, such as `for (int i = 0; i < n; i++)`, and assume a linear relationship between the index variable `i` in such a loop and the loop's boundary condition (i.e., `n`).

- **Why this works**:
  - **Pattern recognition**: By recognizing guard conditions like `i < n`, the tool can infer that the upper bound of `i` is `n`, even if `n` is symbolic. This allows the tool to create an expression for `i` related to `n` during symbolic execution, rather than simply trying every possible value of `i`.
  - **Linear relationship assumption**: Assuming a linear relationship between `i` and `n`, the tool can construct a formula such as `i = k` where `k` goes from `0` to `n-1`. This simplifies the analysis of the loop, because the tool can now use this formula to model the changes of `i` instead of performing full path exploration for every possible value of `n` (achieving the loop's behavior without actually executing each step of the loop).





---

**Handling symbolic indices and loop bounds to resolve path conflicts**. Path conflicts occur when attempting to go beyond the path constraints collected during symbolic tracing of a given PoC (proof of concept) (e.g., trying to write one byte less to adjust the symbolic length). The problem is that a PoC can only concretely traverse one specific path, and any deviation (e.g., different array indices and different numbers of loop iterations) creates new constraints that are incompatible with the previously collected constraints. This is a similar problem to one encountered in previous work [13], which tried to resolve these conflicts through so-called 'path kneading', i.e., temporarily deviating from the path and merging back into the original path at some point to reach the same critical point. This kind of analysis is very time-consuming, taking 2.62 hours on average, which is impractical for a large codebase like the Linux kernel (and after capability exploration, we may need to evaluate hundreds of PoCs per vulnerability).
OK, let's use a concrete example to explain path conflicts and how to handle them.

### Problem Description

Path conflicts occur when attempting to go beyond the path constraints collected during a given PoC (proof of concept). For example, suppose we have a code snippet:

```c
void example_function(int n) {
    char buffer[64];
    for (int i = 0; i < n; i++) {
        buffer[i] = 'A';
    }
}
```

Suppose that during symbolic execution, `n` is concretized to 64. Then the path constraints would be:

1. `0 < n`
2. `1 < n`
3. `2 < n`
4. ...
5. `63 < n`
6. `64 >= n`

These constraints effectively fix `n` to 64, because only when `n` is 64 can all these constraints be satisfied.

If we now try to generate a new PoC by writing one byte less (e.g., letting `n` = 63), this creates new constraints that are incompatible with the previously collected path constraints:

1. `0 < 63`
2. `1 < 63`
3. `2 < 63`
4. ...
5. `62 < 63`
6. `63 >= 63`

These new constraints are obviously different from the original constraints, causing a path conflict.




Our observation is that the over-constraining caused by the concretization of array indices and loop bounds can be easily handled by simply removing these constraints. The rationale is that memory indices change on each run, because the addresses of dynamically allocated objects are unlikely to remain the same, so concretizing symbolic indices by adding constraints in memory access operations forbids the solver from changing the indices, unnecessarily over-restricting the search space. Hence the extra over-constraints. For example, when a write occurs at address 'vul[i/8]' in Figure 3b, S2E introduces a constraint that concretizes the corresponding symbolic address to a value in order to reduce the overhead of modeling symbolic indices. Since we have already abstracted/modeled the vulnerable object (as described in §4.4), these constraints can be automatically detected and simply eliminated. Similarly, suppose that in Figure 6 the parameter n is a symbolic value (during symbolic tracing) whose concrete value is 64, and the loop i increments 64 times, resulting in 65 path constraints 0 < n, 1 < n, ..., 63 < n and 64 >= n, which effectively force n to be 64. Intuitively, when extracting the capability of the loop, we have already modeled the relationship between the loop guard (e.g., n) and the number of times the loop body executes, so discarding these constraints does not cause us to overestimate the capability. In our solution, we simply remove these unnecessary constraints, which allows the solver to search within the valid range of symbolic indices and loop bounds, generating a PoC different from the previously used one (i.e., the system call arguments will change so that the out-of-bounds write and out-of-bounds length fit the target object covering the critical field). In our evaluation, we indeed found that this constraint relaxation approach never caused any problems (e.g., wrong solutions).


```c
void loop(int n) { // 假设 n = 64
    char *vul = (char*)kmalloc(32);
    for (int i = 0; i < n; i++) {
        vul[i] = 0; // OOB Point
    }
}
```


### Existing Solutions and Their Problems

In previous work [13], they tried to resolve these conflicts through "path kneading". The basic idea of path kneading is to temporarily deviate from the path and merge back into the original path at some point to reach the same critical point. This approach is very time-consuming, because it requires extensive computation to find suitable deviation and merge points, taking 2.62 hours on average.

### Our Approach

Our observation is that the over-constraining caused by the concretization of array indices and loop bounds can be easily handled by simply removing these constraints.

### Example Explanation

Suppose we have the following code:

```c
void loop(int n) { // 假设 n = 64
    char *vul = (char*)kmalloc(32);
    for (int i = 0; i < n; i++) {
        vul[i] = 0; // OOB Point
    }
}
```

During symbolic execution, suppose `n` is concretized to 64; we would get the following path constraints:

1. `0 < n`
2. `1 < n`
3. `2 < n`
4. ...
5. `63 < n`
6. `64 >= n`

These constraints force `n` to be 64, leading to over-concretization.

To handle this over-concretization, we can simply remove these constraints. This means we no longer force `n` to be 64, but allow `n` to remain symbolic, so that the solver can explore different values of `n` and their corresponding paths.

By removing these concretization constraints, the solver can explore the following situations:

- `n` can be 63 or other values
- The number of loop iterations is no longer fixed at 64

In this way, we can generate a different PoC in which `n` is no longer restricted to 64 but can take other values. This approach allows us to explore more paths without affecting the exploitation capability.



---

**Eliminating unnecessary constraints**: Due to the complexity of the kernel, the path constraints we collect may be too complex to solve within a limited time budget. To address this problem, some complex constraints introduced by functions (such as `printk()`) that are irrelevant to our goal can be directly ignored. Another special case is race conditions, where system calls with the same arguments are invoked repeatedly, accumulating duplicate constraints. Our system identifies these duplicate constraints in each thread and keeps the last one (when the out-of-bounds access occurs). Since race-condition threads usually repeatedly execute a series of system calls in a loop, we annotate the PoC (proof-of-concept code) at the beginning of each loop to inform our system that the thread is about to re-execute its system call sequence. As shown in Section 6.4, the proposed optimizations significantly improve the efficiency of exploitability evaluation.

---
**Target collection**: We parsed the debug information of the Linux kernel to retrieve all structures, keeping only those containing critical data (such as pointers or reference counters), totaling 2,615. In addition to the type of the critical data, we also collected its offset and the size of the target object, because these constitute the target constraints. Ideally, we should also obtain information about how the target object is used, such as how to allocate it and how to trigger the dereference of its critical data. To this end, we implemented an LLVM pass to build a call graph of the entire kernel, so that we can search for allocation and dereference sites reachable from system calls. However, because the call graph is inaccurate and static analysis does not provide concrete inputs, we still perform exploitability evaluation on each structure, but rely on the call graph to prioritize the order of candidate checks. Meanwhile, we encode knowledge of new target objects during the analysis. In addition, we collected commonly used objects from publicly available exploits (such as `key`, `packet_sock`, `ip_mc_socklist`), which can satisfy most of the exploits we build. SLAKE [20] is a concurrent work recently published for the same purpose, which leverages fuzzing to automatically and systematically generate the required inputs that can lead to the allocation and dereference of a more complete set of kernel objects. KOOBE can directly benefit from the output of such a system.
{% endraw %}
