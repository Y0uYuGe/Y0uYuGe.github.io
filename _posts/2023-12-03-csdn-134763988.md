---
layout: "single"
title: "CTF-PWN-Heap-[The Workflow of malloc and free]"
date: "2023-12-03 17:21:56 +0800"
categories:
  - "CTF and Pwn"
tags:
  - "linux"
  - "Pwn"
  - "CTF"
  - "Heap"
series: "CTF Writeup"
excerpt: "Everything below is based on glibc-2.19; other versions may differ, but this can still serve as a reference."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/134763988"
csdn_article_id: "134763988"
migrated: true
permalink: /posts/csdn-134763988/
redirect_from:
  - "/en/CTF与Pwn/csdn-134763988/"
  - "/CTF与Pwn/csdn-134763988/"
---

{% raw %}
[Link to an expert's knowledge sharing about ptmalloc](https://github.com/jmpews/pwn2exploit/blob/master/PWN%E4%B9%8B%E5%A0%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86.md)

>  Everything below is based on glibc-2.19; other versions may differ, but this can still serve as a reference

# Thoughts on ptmalloc
## The Caching Idea
Understanding the caching idea is really about an idea for improving efficiency

 1. When requesting memory allocation, the system call brk() allocates a relatively large block of memory as a cache; afterwards, even if it cannot be found in the Bins, there is no need to trigger a system call every time — just carve up this large block of memory.
 The caching idea: without caching, every memory allocation would require a system call, resulting in low speed and efficiency
 
 2. When freeing memory, don't return it to the system for the time being; mark it as free, and when the same size is needed next time, use that free block directly, thereby reducing system calls
 3. In addition to the above two kinds of caching, the Fastbin is also a caching mechanism: chunks whose length is within the fastbin range are not put into the Bins after being freed, nor marked as free (not marking as free successfully avoids consolidation). Next time memory is allocated, the Fastbins are searched first, which avoids splitting
 4. The unsorted bin is a cache between freshly freed memory and the bins. Freshly freed memory is first placed in the Unsorted cache; on the next memory allocation, it is searched before the Bins. If a hit in the Unsorted buffer is possible, great; otherwise the chunks in Unsorted are uniformly organized into the corresponding Bins.
 5. last_remainder is also a kind of cache — the leftover piece from splitting, from which the next split also starts. The rough idea is to keep splitting the same chunk. It is used when traversing the Unsorted bin, but its use is conditional.


# Chunk Structure
![Image](/images/csdn/2023/csdn-134763988/image-001-a3cc1e0279.png)

# Large Bin Supplement
For large bins, the split boundaries increase. A simple example: the first 32 large bins all have a split boundary of 64 bytes, and the next 16 large bins have a split boundary of 512 bytes. The above is only for a 32-bit word length
```bash
 32 bins of size      64
 16 bins of size     512
  8 bins of size    4096
  4 bins of size   32768
  2 bins of size  262144
  1 bin  of size what's left
```
Large bins are somewhat special: the storage of free chunks needs to be sorted; large_bin->bk is the chunk with the smallest size, and large_bin->fd is the chunk with the largest size.

# Fast Bin Supplement
When allocating memory, the Fastbins are searched first, and only then the Bins; when freeing memory, if chunk size < max_fast, it is first stored in the Fastbins.
 The consolidation (emptying) of Fastbins is the job of the malloc_consolidate function.
 - When is malloc_consolidate triggered (only in terms of the _int_malloc function)?
 1. The small bins have not yet been initialized
 2. The requested size is larger than small bins

- How does malloc_consolidate consolidate?
It traverses the chunks in the Fastbins, sets each chunk's free flag bit to 0, merges adjacent free chunks, and then places the chunk into the unsorted bin.
Fastbins are singly linked lists; the Fastbins can be traversed via fastbin->fd.

TRIM_FASTBINS=1: fast bins will be merged with the adjacent top chunk; when it is 0, they will not be merged
# Unsorted Bin Supplement
There is only one unsorted bin. When searching for a memory allocation, the Fastbins and small bins are searched first, then the unsorted bin is searched, and all chunks in the unsorted bin are organized into the corresponding Bins. The unsorted bin is located at bin[1].
unsorted_bin->fd points to the head node of the doubly circular linked list, and unsorted_bin->bk points to the tail node of the doubly circular linked list; new nodes are inserted at the head
# Top Chunk Supplement

> For non-main arenas, a relatively large piece of free memory is pre-allocated from the mmap region to simulate a sub-heap, and user requests are answered by managing the sub-heap. Because memory is allocated by address from low to high, at the highest end of the free memory there must be a free chunk, called the top chunk.
> When neither the bins nor the fast bins can satisfy an allocation request, ptmalloc tries to carve a piece of memory out of the top chunk for the user. If the top chunk itself is not large enough, the allocator re-allocates a new sub-heap and migrates the top chunk to the new sub-heap. The new sub-heap is connected to the existing sub-heaps with a singly linked list, and then the required memory is allocated on the new top chunk to satisfy the allocation request.
> In fact, the top chunk is always considered after the fast bins and the bins during allocation, so no matter how large the top chunk is, it will not be placed in the fast bins or the bins. 
> The size of the top chunk constantly changes with allocation and reclamation. Allocating memory from the top chunk causes the top chunk to shrink; if the reclaimed chunk happens to be adjacent to the top chunk, the two chunks merge into a new top chunk, making the top chunk grow. 
> If the memory reclaimed in free is greater than a certain threshold and the size of the top chunk also exceeds the shrink threshold, ptmalloc shrinks the sub-heap. If the top chunk contains the entire sub-heap, ptmalloc calls munmap to return the memory of the entire sub-heap to the operating system.
Since the main arena is the only arena that can map the process's heap region, it can use sbrk() to grow or shrink the process heap. ptmalloc pre-allocates a relatively large piece of free memory at the start (the so-called heap); the main arena's top chunk, on the first call to malloc, allocates a space of (chunk_size + 128KB) aligned to 4KB as the initial heap.
When the user allocates memory from the top chunk, a piece of memory can be taken out directly for the user. When reclaiming memory, if the reclaimed memory happens to be adjacent to the top chunk, they merge into a new top chunk.
When the size of the free memory reclaimed this time reaches a certain threshold and the size of the top chunk also exceeds the shrink threshold, memory shrinking is performed to reduce the size of the top chunk, but at least one page of free memory must be retained, so that memory is returned to the operating system.
If memory is requested from the main arena's top chunk but the top chunk has no free memory, ptmalloc calls sbrk() to move up the process heap boundary brk, and then modifies the size of the top chunk.

# mmaped Chunk Supplement

> When the chunk to be allocated is large enough that neither the fast bins nor the bins can satisfy the request, and even the top chunk itself cannot satisfy the allocation requirement, ptmalloc uses mmap to directly map pages into the process space via memory mapping.
> Chunks allocated this way are directly unmapped when freed, thereby returning the memory to the operating system; referencing such a memory region again will cause a segmentation fault error. Such chunks are not contained in any bin.
# Last Remainder Supplement
> Last remainder is another special chunk; like the top chunk and mmaped chunks, this kind of chunk cannot be found in any bins. When a small chunk needs to be allocated but no suitable chunk can be found in the small bins, if the size of the last remainder chunk is larger than the required small chunk size, the last remainder chunk is split into two chunks: one chunk is returned to the user, and the other becomes the new last remainder chunk.
##  The Generation of Last Remainder
When a bin chain (except fast bins) contains a free chunk, if the size requested by malloc is smaller than the free chunk, this free chunk is split, and the remaining chunk is the last remainder; the last remainder is placed into the unsorted bin

After the last remainder is generated, the last_remainder member pointer in the malloc_state structure is initialized to point to this last_remainder

- Splitting the unsorted bin
  When the unsorted bin has a free chunk that malloc can split and use:
 1. Place the free chunk into the bin chain of the corresponding size
 2. Split the free chunk, producing a last remainder
 3. Put the last remainder into the unsorted bin
- Splitting small bins and large bins
1. Split the free chunk, producing a last remainder
2. Put the last remainder into the unsorted bin

# malloc_state Supplement
> There is only one main arena, but multiple non-main arenas are allowed. The main arena can access the heap region and the mmap region, while non-main arenas can only access the mmap region. Each time, mmap is used to allocate a chunk of memory of a certain size as a sub-heap to simulate a heap. 

# mmap Shrink Threshold
M_TRIM_THRESHOLD is used to set the mmap shrink threshold; the default value is 128KB. Automatic shrinking only happens on free. If the size of the currently freed chunk plus the sizes of the mergeable chunks before and after it is greater than 64KB, and the size of the top chunk reaches the mmap shrink threshold, then for the main arena, malloc_trim() is called to return part of the memory to the operating system; for non-main arenas, heap_trim() is called to return part of the memory to the operating system. When memory shrinking occurs, the mmap allocation threshold and the mmap shrink threshold are also reset.

# mmap Allocation Threshold
M_MMAP_THRESHOLD is used to set the mmap allocation threshold; the default value is 128KB. By default, ptmalloc enables dynamic adjustment of the mmap allocation threshold and the mmap shrink threshold.

When the memory the user needs to allocate is greater than the mmap allocation threshold, ptmalloc's malloc() function is essentially a simple wrapper around mmap(), and the free function is essentially a simple wrapper around munmap(). This is equivalent to allocating memory directly through system calls, and the reclaimed memory is returned directly to the operating system. Because these large blocks of memory cannot be cached and managed by ptmalloc and cannot be reused, ptmalloc only uses this method to allocate memory as a last resort.

# ptmalloc's Workflow for Responding to User Memory Allocation Requests


1) Acquire the arena lock. To prevent multiple threads from accessing the same arena simultaneously, the arena lock must be acquired before allocation. The thread first checks whether an arena already exists in the thread's private instance; if it exists, it tries to lock that arena. If the lock succeeds, that arena is used to allocate memory; otherwise, the thread searches the arena circular linked list for a free (unlocked) arena. If all arenas are already locked, ptmalloc creates a new arena, adds it to the global arena circular linked list and the thread's private instance, locks it, and then uses that arena for the allocation operation. A newly created arena must be a non-main arena, because the main arena is inherited from the parent process. When creating a non-main arena, mmap() is called to create a sub-heap, and the top chunk is set up.

2) Convert the user's requested size into the size of the chunk space that actually needs to be allocated. See the request2size macro for details (malloc.c:3332)

3) Check whether the size of the chunk to be allocated satisfies chunk_size <= max_fast (max_fast defaults to 64B). If so, go to the next step; otherwise jump to step 5. (malloc.c:3340)

4) First try to find a chunk of the required size in the Fastbins to allocate to the user. If one can be found, the allocation ends. Otherwise go to the next step. (malloc.c:3340)

5) Check whether the required size falls within the small bins, i.e., check whether chunk_size < 512B holds. If the chunk size falls within the small bins, go to the next step; otherwise go to step 7. (malloc.c:3377)

6) According to the size of the chunk to be allocated, find the specific small bin where it belongs, and remove a chunk of exactly the required size from the tail of that Bin. If successful, the allocation ends; otherwise, go to 8. (malloc.c:3377)

7) Reaching this step means a large block of memory needs to be allocated, so ptmalloc first traverses the chunks in the Fastbins, merges adjacent free chunks, and links them into the unsorted bin. The consolidation of the Fastbins is handled by `malloc_consolidate`. (malloc.c:3421)

8) Then traverse the unsorted bin; if there is a chunk of exactly the right size, take it and use it directly, ending the traversal loop. (The rest is left alone, kept as-is)
If the size is not exact, put it into the corresponding bin: if it is a small bin, put it into the small bin; if it is a large bin, put it into the large bin (fast bins are contained within small bins)
After traversing all chunks, since there is no chunk of exactly the right size, go to the bins to which chunks were just distributed to search. If the size belongs to a small bin, search in the small bin; if there is a chunk larger than the required size, split it. If the leftover part of the split is not less than 0x10, put it into the unsorted bin; if it is less, give it all together. If there is no chunk in the small bin larger than the required size, go search in the large bin (this is the case of splitting in the small bin).
In the large bin, traverse in reverse order (starting from the bk pointer). If a chunk larger than the required size is found, split it the same way; if the leftover part of the split is not less than 0x10, put it into the unsorted bin; if less, give it all together.
If still nothing is found, go to the next step

10) Reaching this step means no chunk of suitable size was found in the corresponding bin, whether small bin or large bin. For a small bin, if there is no small bin of the corresponding size, you can only do idx+1. For a large bin, the large bin in the previous step may not necessarily have a suitable chunk to split, because the gaps between large bins are large. Suppose the large bin at the current idx has only one chunk, but the required size is larger than that chunk — this makes it impossible to find a suitable one, so you can only continue idx+1. In the end, you need to find the first non-empty bin afterwards based on the bitmap. The chunks in the bin found in these two cases can definitely be split or fully allocated (the remaining size < MINSIZE) (malloc.c:3649)

11) If no suitable chunk has been found yet, the top chunk needs to be used for allocation. Check whether the size of the top chunk satisfies the required chunk size; if so, carve a chunk out of the top chunk. Otherwise go to the next step. (malloc.c:3749)

12) Reaching this step means the top chunk cannot satisfy the allocation request either, so there are two choices: if it is the main arena, check whether this is the first call; if so, initialization work needs to be performed once, allocating a space of (chunk_size + 128KB) aligned to 4KB as the initial heap. If it has already been initialized, call sbrk() to increase the top chunk size, then carve a chunk out of the top chunk. Carve a chunk out of the top chunk so that it satisfies the allocation requirement, and return the memory pointer to the user. If it is a non-main arena, check whether the size of the chunk to be allocated is greater than or equal to the mmap allocation threshold. If so, go to the next step; otherwise call mmap to allocate a new sub-heap, increase the top chunk size, then carve a chunk out of the top chunk so that it satisfies the allocation requirement, and return the memory pointer to the user. Here, the chunk size determines which method to use. 

13) Use mmap() to allocate directly: use the mmap system call to map a space of chunk_size aligned to 4kB into the program's memory space. Then return the memory pointer to the user.

# Workflow of free
1) The free() function likewise needs to acquire the arena lock first to ensure thread safety.

2) Check whether the passed-in pointer is 0; if it is 0, do nothing and return directly. Otherwise go to the next step.

3) Check the size and location of the chunk. If chunk_size <= max_fast and the chunk is not located at the top of the heap — that is, it is not adjacent to the Top chunk — go to the next step; otherwise jump to step 5. (Because a chunk (fastbin) adjacent to the top chunk will be merged with the top chunk, here you need to check not only the size but also the adjacency)

4) Put the chunk into the Fastbins. When a chunk is placed into the Fastbins, the chunk's in-use status bit P is not modified, nor is it merged with adjacent chunks. It is just put in, that's all. After this step is done, the freeing ends, and the program returns from the free() function.

5) Check whether the chunk to be freed is an mmaped chunk; if so, call munmap() to free the mmaped chunk, unmapping the memory space so that the space is no longer valid. If it is not a mapped chunk, and the dynamic adjustment mechanism of the mmap allocation threshold is enabled, and the size of the currently reclaimed chunk is greater than the mmap allocation threshold, set the mmap allocation threshold to the size of this chunk and set the mmap shrink threshold to twice the mmap allocation threshold; freeing is complete. Otherwise jump to the next step.

6) Check whether the previous chunk is in use; if the previous chunk is also free, merge them, and go to the next step.

7) Check whether the next chunk after the currently freed chunk is the top chunk; if so, go to step 9; otherwise go to the next step.

8) Check whether the next chunk is in use; if the next chunk is also free, merge them, and put the merged chunk into the unsorted bin. Note that during the merge process, the chunk size must be updated to reflect the size of the merged chunk. Go to step 10.

9) If this step is reached, it means a chunk adjacent to the top chunk was freed. No matter how large it is, merge it with the top chunk, and update information such as the size of the top chunk. Go to the next step. (malloc.c:3950)

10) Check whether the size of the merged chunk is greater than FASTBIN_CONSOLIDATION_THRESHOLD (default 64KB); if so, a Fastbins consolidation operation (malloc_consolidate) is triggered: the chunks in the Fastbins are traversed and merged with adjacent free chunks, and the merged chunks are put into the unsorted bin. The Fastbins become empty. After the operation is complete, go to the next step.

11) Check whether the size of the top chunk is greater than the mmap shrink threshold (default 128KB); if so, for the main arena, it tries to return part of the top chunk to the operating system. But the first 128KB of space allocated will not be returned; ptmalloc manages this part of memory all along to respond to user allocation requests. If it is a non-main arena, sub-heap shrinking is performed, returning part of the top chunk to the operating system; if the top chunk is the entire sub-heap, the entire sub-heap is returned to the operating system. After this step is done, freeing ends, and the free() function exits. It can be seen that the conditions for shrinking the heap are that the size of the currently freed chunk plus the sizes of the mergeable chunks before and after it is greater than 64k, and the size of the top chunk must reach the mmap shrink threshold for the heap to possibly be shrunk.
{% endraw %}
