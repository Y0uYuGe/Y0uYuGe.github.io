---
layout: "single"
title: "制作一个RISC-V的操作系统五-RISC-V汇编语言编程六（条件分支指令 伪指令 无条件跳转指令 问题 指令寻址模式总结）"
date: "2024-03-14 11:06:20 +0800"
categories:
  - "操作系统与体系结构"
tags:
  - "risc-v"
  - "算法"
excerpt: "B-type：B指代branch6-11中：先是第11位然后才是1到4位24-31中：先是5到10位，然后才是第12位符号扩展，所以是-20482和20472标号：标签类似地址写loop不写地址值汇编结果无条件跳转指令以寄存器值为基址来偏移跳转AUPIC会构造一个32位的数，将IMM作为其高20位，并存放到X6中，然后JALR跳转到X6+IMM的低12位的值。所以整体来看就是跳转到X6+一个32位数值"
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/136442547"
csdn_article_id: "136442547"
migrated: true
---

{% raw %}
@[toc]
# 条件分支指令
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-001-f2e14eb126.png)
B-type：B指代branch
6-11中：先是第11位然后才是1到4位
24-31中：先是5到10位，然后才是第12位

符号扩展，所以是-2048*2和2047*2

标号：标签类似地址
# 伪指令
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-002-4910d22d41.png)
写loop不写地址值
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-003-38a2da02d1.png)

  ![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-004-d7c36a48fa.png)

```c
char array[] = {'h', 'e', 'l', 'l', 'o', ',', 'w', 'o', 'r', 'l', 'd', '!', '\0'};
int len = 0;
while (array[len] != '\0') {
 len++;
}
```
汇编结果

```bash
.data
array: .byte 'h', 'e', 'l', 'l', 'o', ',', 'w', 'o', 'r', 'l', 'd', '!', 0

.text
.globl _start
_start:
    la t0, array     # 加载数组的地址到寄存器 t0
    li t1, 0         # 初始化计数器 t1 为 0

loop:
    lbu t2, 0(t0)    # 从内存中读取数组元素到寄存器 t2
    beqz t2, end     # 如果读取到的元素值为 0，即结束标记 '\0'，则跳转到 end 标签

    addi t1, t1, 1   # 计数器加1
    addi t0, t0, 1   # 数组指针移动到下一个元素
    j loop           # 无条件跳转到 loop 标签继续循环

end:
    # 此时计数器 t1 中存储的即为数组长度
    # 可以在该处添加代码来利用长度进行其他操作
    # 例如打印数组长度，或进行其他处理
    li a0, 1         # 将系统调用编号 1 (print integer) 装入寄存器 a0
    move a1, t1      # 将计数器 t1 中的值装入寄存器 a1
    ecall            # 发起系统调用

    # 程序结束，退出
    li a7, 10        # 将系统调用编号 10 (exit) 装入寄存器 a7
    li a0, 0         # 将返回值 0 装入寄存器 a0
    ecall            # 发起系统调用
```

# 无条件跳转指令
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-005-3d473c0a53.png)
以寄存器值为基址来偏移跳转
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-006-015d62351f.png)
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-007-23c54e69cb.png)
# 问题
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-008-4c4bd3a813.png)
AUPIC会构造一个32位的数，将IMM作为其高20位，并存放到X6中，然后JALR跳转到X6+IMM的低12位的值。所以整体来看就是跳转到X6+一个32位数值
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-009-719eefa92b.png)
# 指令寻址模式总结
![在这里插入图片描述](/images/csdn/2024/csdn-136442547/image-010-1a86c23093.png)
{% endraw %}
