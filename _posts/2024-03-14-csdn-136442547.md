---
layout: "single"
title: "Building a RISC-V Operating System V - RISC-V Assembly Language Programming VI (Conditional Branch Instructions, Pseudo-Instructions, Unconditional Jump Instructions, Question, Summary of Instruction Addressing Modes)"
date: "2024-03-14 11:06:20 +0800"
categories:
  - "Operating Systems and Architecture"
tags:
  - "risc-v"
  - "Algorithms"
excerpt: "B-type: B stands for branch. In bits 6-11: first bit 11, then bits 1 to 4. In bits 24-31: first bits 5 to 10, then bit 12. Sign-extended, so it is -2048*2 and 2047*2. Label: a label is like an address. Write loop instead of an address value. Assembly result. Unconditional jump instructions jump with an offset relative to a register value as the base. AUPIC constructs a 32-bit number, uses IMM as its upper 20 bits, and stores it in X6; then JALR jumps to the value of X6 plus the lower 12 bits of IMM. So overall, it jumps to X6 plus a 32-bit value."
source: "CSDN"
original_url: "https://blog.csdn.net/llovewuzhengzi/article/details/136442547"
csdn_article_id: "136442547"
migrated: true
permalink: /posts/csdn-136442547/
redirect_from:
  - "/en/操作系统与体系结构/csdn-136442547/"
  - "/操作系统与体系结构/csdn-136442547/"
---

{% raw %}
@[toc]
# Conditional Branch Instructions
![Image](/images/csdn/2024/csdn-136442547/image-001-f2e14eb126.png)
B-type: B stands for branch
In bits 6-11: first bit 11, then bits 1 to 4
In bits 24-31: first bits 5 to 10, then bit 12

Sign-extended, so it is -2048*2 and 2047*2

Label: a label is like an address
# Pseudo-Instructions
![Image](/images/csdn/2024/csdn-136442547/image-002-4910d22d41.png)
Write loop instead of an address value
![Image](/images/csdn/2024/csdn-136442547/image-003-38a2da02d1.png)

  ![Image](/images/csdn/2024/csdn-136442547/image-004-d7c36a48fa.png)

```c
char array[] = {'h', 'e', 'l', 'l', 'o', ',', 'w', 'o', 'r', 'l', 'd', '!', '\0'};
int len = 0;
while (array[len] != '\0') {
 len++;
}
```
Assembly result

```bash
.data
array: .byte 'h', 'e', 'l', 'l', 'o', ',', 'w', 'o', 'r', 'l', 'd', '!', 0

.text
.globl _start
_start:
    la t0, array     # 加载数组的地址到寄存器 t0
    li t1, 0         # 初始化计数器 t1 为 0

loop:
    lbu t2, 0(t0)    # 从内存中读取数组元素到寄存器 t2
    beqz t2, end     # 如果读取到的元素值为 0，即结束标记 '\0'，则跳转到 end 标签

    addi t1, t1, 1   # 计数器加1
    addi t0, t0, 1   # 数组指针移动到下一个元素
    j loop           # 无条件跳转到 loop 标签继续循环

end:
    # 此时计数器 t1 中存储的即为数组长度
    # 可以在该处添加代码来利用长度进行其他操作
    # 例如打印数组长度，或进行其他处理
    li a0, 1         # 将系统调用编号 1 (print integer) 装入寄存器 a0
    move a1, t1      # 将计数器 t1 中的值装入寄存器 a1
    ecall            # 发起系统调用

    # 程序结束，退出
    li a7, 10        # 将系统调用编号 10 (exit) 装入寄存器 a7
    li a0, 0         # 将返回值 0 装入寄存器 a0
    ecall            # 发起系统调用
```

# Unconditional Jump Instructions
![Image](/images/csdn/2024/csdn-136442547/image-005-3d473c0a53.png)
Jump with an offset relative to a register value as the base
![Image](/images/csdn/2024/csdn-136442547/image-006-015d62351f.png)
![Image](/images/csdn/2024/csdn-136442547/image-007-23c54e69cb.png)
# Question
![Image](/images/csdn/2024/csdn-136442547/image-008-4c4bd3a813.png)
AUPIC constructs a 32-bit number, uses IMM as its upper 20 bits, and stores it in X6; then JALR jumps to the value of X6 plus the lower 12 bits of IMM. So overall, it jumps to X6 plus a 32-bit value.
![Image](/images/csdn/2024/csdn-136442547/image-009-719eefa92b.png)
# Summary of Instruction Addressing Modes
![Image](/images/csdn/2024/csdn-136442547/image-010-1a86c23093.png)
{% endraw %}
